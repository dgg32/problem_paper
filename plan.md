# Fraud Academic Paper Scanner — POC Plan

**Scope:** Biology / microbiology papers only (the author's field).
**Status:** Proof of concept. Goal is a working end-to-end pipeline on a small slice of data, not production coverage.
**Related:** Obsidian note `Projects/paper-fraud-scanner/Fraud Academic Paper Scanner Plan`.

---

## 0. Framing and guardrails (read first)

Before any code, three principles that shape every design decision below.

1. **A retraction is not proof of fraud.** The Retraction Watch database mixes deliberate misconduct (paper mills, image manipulation, fabricated data) with honest error, publisher error, plagiarism disputes, and expression-of-concern notices that were later reinstated. In the dataset: of 71,106 records, 3,581 are "Expression of concern", 1,494 are "Correction", and 160 are "Reinstatement" — **not** retractions at all. So the schema must **never** carry a boolean `known_fraud: true` on a person. That is a defamatory claim about a real, named individual.

2. **Model facts, not verdicts.** Store what is verifiable ("this paper was retracted on date X for reason Y") and derive risk *scores* from it. A score is a prioritization signal for a human reviewer, never a conclusion. Every output of this system is a **hypothesis for human review**, which is exactly why step 5 of the original plan (human review table) is the right final gate.

3. **The output is a triage queue, not an accusation.** Framing the UI as "papers a human should look at first, and why" keeps the project defensible, useful, and honest. We surface 🚩 evidence and let a domain expert judge.

4. **Same person ≠ same responsibility.** Once identity is resolved across papers (§2.1b), a person-level signal (e.g. "this probable person has a misconduct-adjudicated paper somewhere in their record") must **never** be read as a claim about any *other specific paper* of theirs. Author role varies paper to paper — 1st/corresponding author carries different responsibility than a middle author of 17, and papers can be decades apart. **Worked example (confirmed 2026-07-17):** Hiroshi Asakura (朝倉 宏, verified same person via researchmap.jp + J-GLOBAL ID 200901058605650763) is **1st author** on a 2021 paper retracted for fabrication (a formal misconduct finding), and a **middle author** (8th of 17) on an unrelated 2001 paper 20 years earlier. Confirming "same person" answers identity; it says nothing about the 2001 paper's risk. **Consequence:** any person-level derived flag (`cluster_adjudicated`, and future Tier-A/Phase-5 signals) must carry a pointer to the *specific* paper(s)/DOI(s) that produced it (`cluster_misconduct_dois`) and, wherever author role is available, show it alongside the flag — never surface the boolean alone.

These are not just ethics footnotes — they change the data model (no fraud boolean), the labels used for ML (retracted-for-misconduct-reason vs. all-retractions), and the UI copy ("flagged for review", not "fraudulent").

---

## 1. Data reality check (from the actual CSV)

Numbers below are measured from `retraction_watch/retraction_watch.csv` (71,106 rows).

| Slice | Count |
|---|---|
| All records | 71,106 |
| Life-science `(BLS)` + Microbiology | 23,991 |
| — of which Microbiology | 2,100 |
| Bio rows with an original-paper DOI | 22,992 (96%) |
| Bio rows with an original-paper PubMed ID | 23,508 (98%) |

**Useful columns:** `Title`, `Subject`, `Institution`, `Journal`, `Publisher`, `Country`, `Author` (semicolon-separated names), `OriginalPaperDOI`, `OriginalPaperPubMedID`, `OriginalPaperDate`, `RetractionDate`, `RetractionNature`, `Reason` (semicolon-separated controlled vocabulary), `Notes`.

**Data quirks to handle in the loader:**
- `Author` is a `;`-separated string of display names — **no ORCID, no per-author affiliation, no email** in this file. ORCID must be resolved later (PubMed/OpenAlex). So `ORCID` on the author node starts empty and gets enriched.
- **Author↔institution is a pool, not a mapping.** `Institution` is a `;`-separated, *deduplicated* list of the paper's departments/institutions — it does **not** align with the author list by position or count. Measured on the 23,991 bio rows: only 8.8% have equal author/institution counts (mostly coincidence), 32.8% list a single institution for many authors, 58.4% have mismatched counts. Example: 10 authors → 2 institutions; 7 authors → 5 (shared-department) institutions. **Consequence:** you cannot fill a per-author `affiliation` (or `email`) on the `WROTE` edge from this CSV. Treat institutions as a **paper-level** fact (`(Paper)-[:INVOLVES]->(Institution)`); a clean `(Author)-[:AFFILIATED_WITH]->(Institution)` edge only comes from OpenAlex/PubMed enrichment in Phase 2.
- `Reason` uses a controlled vocabulary. Top misconduct-signal reasons: `Paper Mill` (11,796), `Compromised Peer Review` (11,421), `Duplication of/in Image` (5,458), `Euphemisms for Plagiarism`, `Fabrication`. Split these on `;` and store as discrete labels — they are the strongest features for step 2.
- Dates are `M/D/YYYY H:MM`. Some are `0000` / "unknown". Parse defensively.
- Affiliation lives in `Institution` (per paper, not per author) — semicolon-separated list of departments.
- Names are not disambiguated. "J Smith" collisions are real. Treat name-only matching as weak evidence; prefer DOI/PMID and (later) ORCID for identity.

---

## 2. Architecture overview

```
                          ┌──────────────────────────┐
  Retraction Watch CSV ─▶ │  Loader / normalizer      │
                          └───────────┬───────────────┘
                                      ▼
  OpenAlex (primary) ─enrich─▶ ┌──────────────────┐   ┌────────────────────────┐
  PubMed  (fallback)           │  Graph DB        │─▶ │ Risk scoring engine    │
  (co-authored papers)         │ (LadybugDB/Neo4j)│   │  - graph features      │
                               └──────────────────┘   │  - flag sensors(skills)│
                                                       └──────────┬─────────────┘
                                                                  ▼
                                                          suspicious papers
                                                                  │
                      paperconan (Claude skill) ◀─ deep AI ───────┤
                      run in a routine or                         ▼
                      interactive session         ┌────────────────────────┐
                                                  │ FastAPI + HTMX table    │
                                                  │  (human review queue)   │
                                                  └────────────────────────┘
```

**Graph DB choice.** For a single-user embedded POC, use **LadybugDB** (`pip install ladybug`) — an embedded, serverless property-graph DB with Cypher and a Python API (formerly Kùzu; same engine, renamed — https://github.com/LadybugDB/ladybug). No server to run. If you instead want the Neo4j Browser/Bloom visualizations, use **Neo4j Community** in Docker. The schema below is engine-agnostic, so switching later is cheap. **Recommendation: start with LadybugDB.**

**Update (2026-07-17): default switched to Neo4j for now.** All of §2.1b/c (author identity + targeted expansion) is implemented and running live on Neo4j — Browser/Cypher exploration turned out valuable throughout this build (see the worked examples in §2.1b), and the ~15k-node scale here doesn't need an embedded engine. `.env.yaml` `graph_db.engine` now defaults to `"neo4j"`. The LadybugDB-vs-Neo4j analysis below is kept as-is (still the right reasoning for Tier B/C); this is a "for now" pick, not a reversal of that analysis — Ladybug remains a valid option to revisit.

Graph-analytics capability of each engine, relevant to Phase 2 (verified against Ladybug's docs):
- **LadybugDB has built-in graph algorithms** via its `algo` extension — **PageRank, Weakly/Strongly Connected Components, Louvain (community detection), K-Core, shortest paths.** These cover the Phase 2 **Tier-A** heuristic features (co-author components, distance-to-retracted-author, clusters) natively; no need to hand-roll them.
- **Result export:** `get_as_df()` (pandas), `get_as_pl()` (polars), `get_as_arrow()` (PyArrow), plus CSV/Parquet/JSON.
- **No documented direct PyG/DGL/NetworkX exporter** (old Kùzu had `get_as_torch_geometric()`; current Ladybug docs don't list it). So for **Tier B/C** (node2vec, GNN) export an edge list via `get_as_df()` and build the NetworkX/PyG graph in Python — routine, just not one call.
- **What Neo4j adds (the real gap):** Neo4j's **GDS** library provides a turnkey in-database **node classification pipeline** (`gds.beta.pipeline.nodeClassification`) plus node embeddings (FastRP, node2vec, GraphSAGE, HashGNN). **LadybugDB has neither** — its `llm`/`vector` extensions do text embeddings and similarity search, not graph node embeddings, and there is no supervised training/prediction. So the node-property-prediction goal (Phase 2 Tier B/C) is the one thing that genuinely favors Neo4j. **Decision: start on LadybugDB (Tier A needs no ML), and revisit engine choice at Tier B/C** — see Phase 2. (File I/O is not a factor either way — the pipeline needs none of GraphML/CSV/Parquet specifically.)

**Language/stack:** Python throughout (loader, enrichment, scoring, backend). FastAPI + HTMX + Jinja2 for the frontend. This keeps skills, scoring, and web in one runtime.

---

## 3. Graph schema (revised)

The original `(author)-[:WRITES]->(paper)` is the right spine. Revisions: drop the fraud boolean, add nodes for the entities we actually want to reason over, and attach retraction as facts.

**Nodes**
- `AuthorInstance { instance_id, name, name_key, orcid?, orcid_raw?, has_orcid, orcid_source, cluster_id, cluster_size, coherence_outlier, on_misconduct_paper, misconduct_reasons[], cluster_adjudicated, identity_override? }` — **one node per authorship** (one author slot on one paper); authors are **never merged** (rationale + pipeline in §2.1b). Supersedes the original merged `Author` node. `orcid` is kept as OpenAlex assigned it (a *low-trust* signal — OpenAlex mis-assigns); a human `split` preserves the original in `orcid_raw`. Same-person identity is a separate, **reversible** edge layer (`PROBABLY_SAME_AS`), not a node merge; `cluster_id` is the derived *probable person*. **No `known_fraud`.** Adjudication is a *sourced per-instance fact* `on_misconduct_paper` (wrote a paper retracted for `Misconduct - Official Investigation(s) and/or Finding(s)` or `Investigation by ORI`; full chain in `(Paper)-[:RETRACTED_FOR]->(Reason)`) **plus** a *derived, reviewable* cluster signal `cluster_adjudicated` — **never a boolean stamped on a person** (§0). Never set from retraction counts or model scores. (201 adjudicated probable-persons in the microbiology seed.)
- `Paper { doi, pmid, title, published_date, is_retracted (bool), retraction_date?, retraction_nature? }` — `is_retracted` is a *fact* from the source.
- `Journal { name, publisher? }`
- `Institution { name, country? }`
- `Reason { code }` — one node per controlled-vocabulary reason (`Paper Mill`, `Duplication of/in Image`, …).

**Relationships**
- `(AuthorInstance)-[:WROTE { author_position?, is_corresponding, affiliation?, email? }]->(Paper)` — now **1:1** (one authorship per instance). `affiliation` is the per-author byline, **populated** from OpenAlex enrichment (cross-checked vs PubMed, which agrees on names/affiliations); `email` still unavailable.
- `(AuthorInstance)-[:AFFILIATED_WITH]->(Institution)` — **per-authorship byline** institutions (ROR), from OpenAlex enrichment (Phase 2); not derivable from the CSV.
- `(AuthorInstance)-[:PROBABLY_SAME_AS { confidence, basis, orcid_conflict, shared_coauthors, shared_institutions, name_match }]->(AuthorInstance)` — the **reversible same-person identity layer** (§2.1b); weighted hypotheses, never a merge.
- `(Paper)-[:INVOLVES]->(Institution)` — paper-level, from the CSV Institution pool (see §1: not per-author).
- `(Paper)-[:PUBLISHED_IN]->(Journal)`
- `(Paper)-[:RETRACTED_FOR]->(Reason)` (only for retracted papers)
- `(Paper)-[:CITES]->(Paper)` — from the *targeted* expansion (§2.1c), not blanket enrichment: edges from each candidate paper to any paper already in the graph. Powers skill "cited a retracted paper" (Phase 4 #1) and the Tier-A "citations to retracted papers" feature. **Built for the top-N target authors only** (not the whole seed set).

Derived/queryable signals this enables:
- Co-authorship proximity to authors of retracted papers (graph distance).
- Journals/publishers with high retraction rate.
- Institutions clustered in paper-mill retractions.
- Whether a *new* downloaded paper cites known-retracted work.

---

## 4. Phases

### Phase 1 — Build the seed graph (from CSV)
**Deliverable:** populated graph of retracted bio papers + their authors/journals/reasons.

1. Filter CSV to `(BLS)` + Microbiology rows (~24k). Optionally start even smaller: **Microbiology only (2,100 rows)** for the first pass to keep everything fast and inspectable.
2. Normalize: split `Author`, `Reason`, `Subject`; parse dates; canonicalize DOIs (lowercase, strip `https://doi.org/`); build `name_key`.
3. Upsert nodes/edges. Mark all these papers `is_retracted = true`.
4. Sanity queries: top journals by retraction count, top reasons, biggest co-author components.

*Exit criteria:* can answer "who co-authored the most retracted microbiology papers, and in which journals" via one query.

### Phase 2 — Enrich + predict (the ML/graph-algorithm core)
**Deliverable:** a ranked list of *not-yet-retracted* papers that resemble retracted ones.

1. **Enrich — OpenAlex primary, PubMed secondary.** Metadata enrichment (ORCID, per-author affiliation) for each seed paper's authors — done for all 1,631 seed papers. Growing the graph *beyond* retractions with authors' other papers + `CITES` edges is **targeted, not blanket** — see §2.1c below, which supersedes the original "pull each seed author's other papers" framing.
   - **OpenAlex (`https://api.openalex.org`) — primary.** Free, no API key; just pass `mailto=<your email>` to join the "polite pool". Rate limit ~100k calls/day, ~10 req/s. One JSON call per work returns the three things the CSV lacks together: **per-author affiliations** (`authorships[].institutions`), **ORCID** (`authorships[].author.orcid`), and **citation edges** (`referenced_works` → outgoing `CITES`; `cited_by_api_url` → incoming). Look up directly by DOI (`/works/doi:<doi>`), PMID (`/works/pmid:<id>`), or ORCID. Cursor-paginate an author's works with `cursor=*`, 200/page.
   - **PubMed E-utilities — secondary / fallback.** Use for records OpenAlex is missing (your CSV has 98% PMID coverage to key off) and to cross-check. Returns XML; API key optional (raises rate 3→10 req/s). Note its citation links (`elink`) are incomplete — don't rely on PubMed for `CITES` edges; that's OpenAlex's job.
   - Both credentials/params live in `.env.yaml` (see repo root). OpenAlex needs only an email; PubMed key is optional.
   - **Measured findings from the seed enrichment run (2026-07-17, `graph_processing/extract_enrich.py`, 1,631 clean microbiology papers):**
     - **PubMed's real role is record/author-list rescue, *not* ORCID top-up.** Across all 1,631 papers PubMed filled **0** missing ORCIDs — OpenAlex's ORCID coverage was a strict superset of PubMed's. PubMed (and then the CSV `Author` column) mattered only for rescuing *author lists* on ~10 withdrawn papers whose author metadata was scrubbed upstream. So keep the fallback chain **OpenAlex → PubMed → CSV**, but expect PubMed to add identities/authorship, not ORCIDs.
     - **ORCID coverage ≈ 66%** (5,064 of 7,730 authors). The other third have no ORCID and can only be merged by normalized name (weak identity) — this bounds how aggressively author disambiguation / co-author features (below) can rely on hard IDs.
     - **Data hygiene matters:** 386 of 2,035 nominally-microbiology rows carried an invalid PMID (`0`) and 41 a sentinel DOI (`unavailable`); the loader must require a real DOI (`10.…`) **and** a numeric non-zero PMID or it produces author-less orphan papers.

   **1b. Author identity — instance-based, NO merge (added 2026-07-17; implemented on Neo4j).** Two distinct identity problems drove this redesign, which replaces the merged `Author` node:
   - **Problem A — fragmentation:** one real person appears as many nodes (ORCID'd + name-only, or name spelled differently). *Under-merging.*
   - **Problem B — mis-assignment:** OpenAlex sometimes stamps the *wrong* same-named person's ORCID onto a paper, attaching a retraction to an innocent researcher — the §0 defamation risk. **Confirmed live:** a "Bing Liu" dental retraction carried a materials-scientist's ORCID (verified against his own ORCID record, which claimed only a *Coatings* paper).

   Because every source is fallible — ORCID employment fields are optional, PubMed has same-name collisions, OpenAlex mis-assigns — **we never merge authors.** Design:
   - **One `AuthorInstance` per authorship** (10,050); nothing is collapsed, *not even by ORCID*.
   - **Identity = a reversible weighted edge layer** `PROBABLY_SAME_AS`. ORCID is a *low-trust* signal (Problem B), so a **conflicting ORCID lowers confidence, it does not veto** — and the low-confidence "conflict + shared co-authors" case is itself the mis-assignment signature to review. Confidence tiers (hand-set **ranking priors**, not calibrated probabilities — their job is ordering for the cluster threshold):

     | basis | conf | signal |
     |---|---|---|
     | `orcid_match` | 0.97 | same ORCID (strongest; <1.0 because OpenAlex can mis-assign) |
     | `name_coauthor` | 0.70–0.90 | same name + shared co-authors |
     | `name_institution` | ~0.50 | same name + shared institution |
     | `name_only` | ~0.35 | same name, nothing else |
     | `name_orcid_conflict` | 0.05–0.14 | *different* ORCIDs — lowest, still recorded |

   - **Hybrid edge structure:** same-ORCID groups are a spanning tree (k-1 edges, lossless at flat 0.97), name links stay full pairwise. Cuts Raoult (227 instances) from 25,651 clique edges to 226 with *identical* clusters; 5,133 edges total.
   - **Soft resolution:** weakly-connected-components over edges ≥ threshold (default 0.70) → a **recomputable** `cluster_id` (the "probable person"); 7,713 clusters. Downstream features resolve over clusters, never over a physical merge.
   - **Coherence flag (review-only):** the conflict tier can't see a *same*-ORCID mis-assignment (an innocent given a prolific namesake's ORCID clusters at 0.97, no conflict). Such an instance betrays itself by sharing **neither** a co-author **nor** an institution with its cluster → `coherence_outlier=true`, ranked by cluster size. **It is a hypothesis, never an action — a legitimate institution move is structurally identical to a mis-assignment.** Worked example: **Ashok Pandey** was flagged as a size-12 outlier (a 2019 IITR paper amid NIIST papers); his CV confirms he *moved* CSIR-NIIST → IITR in 2017 — a **false positive**, a real career change. Cheap resolver: the ORCID-record check (`orcid_client.py`) — does his ORCID *claim* the paper? (Note: an empty/incomplete ORCID works list can't disprove authorship — measured false-positive trap; ~66% ORCID coverage, records often sparse.)
   - **Adjudication re-expressed at the right grain:** per-instance fact `on_misconduct_paper` (+ `misconduct_reasons`) with the full chain `(:AuthorInstance)-[:WROTE]->(:Paper)-[:RETRACTED_FOR]->(:Reason)`, plus a derived, reviewable `cluster_adjudicated` on the probable person — always paired with `cluster_misconduct_dois` (the specific triggering paper(s), added 2026-07-17 after the Hiroshi Asakura case; see §0 principle 4 — same person ≠ same responsibility, and author role differs paper to paper). 201 adjudicated probable-persons — matches the old merged count, with **0** resting solely on an unreviewed coherence-outlier instance.
   - **Confirmations (added 2026-07-17):** a third override category alongside splits/merges — a human reviews a `coherence_outlier` and confirms it's genuinely one person (career move, not mis-assignment); no graph change needed, but the decision is still recorded (`identity_overrides.yaml` → `confirmations`, keyed by ORCID) so the same pair doesn't keep resurfacing in the review report on every re-run. Sets `coherence_reviewed`/`coherence_verdict` on the node; the raw `coherence_outlier` signal is left untouched. Two confirmed cases so far: **Ashok Pandey** (CSIR-NIIST → IITR, 2017) and **Hiroshi Asakura / 朝倉宏** (Obihiro University → National Institute of Health Sciences; verified via researchmap.jp profile `read0164939` and J-GLOBAL ID `200901058605650763`) — both false positives on the coherence flag, i.e. the flag is working as a review trigger, not an accusation.
   - **Batch review of all 31 strong outliers (completed 2026-07-17).** Worked through every remaining `cluster_size>=3` unreviewed coherence outlier from the targeted-expansion run (`import_cypher.txt` §5g). **Method refinement:** (1) first ask the ORCID's own claimed-works list (`orcid_client.py --has-doi`) whether it claims the flagged DOI — 7 cases resolved this way as direct first-party confirmations (Yuen, Malafaia, Ko, Kumar, Rajendran, Ahmad, and one of Zhang's two flagged papers); (2) where ORCID was silent/sparse (the common case — ~66% coverage), cross-checked the flagged paper's actual per-author institution (OpenAlex `authorships`) against the cluster's core institution/specialty — a match strongly indicates the coherence flag is mis-triggering on sparse coauthor-overlap data rather than a real mis-assignment (11 more cases confirmed this way: Collins's 1985 RSV paper, Pease, Ćirić, DiNapoli, Umar, Asad, Yang, Xu, Matsuyama, Sarker, Mandal), while a clean institution *mismatch* is strong split evidence (14 splits: Collins's Sorafenib/TACE2 paper — a different, UK-based MBBS clinician; Zhang's JAMA anesthesia paper — not even in the trial's named author list, a "RAGA Study Investigators" collaborator-group name collision; all 4 Shin-Hee Kim outliers — 4 *different* institutions from each other, an extreme common-name collision; all 4 Naoki Mori outliers — Rockefeller/Kyoto Pharma/USC, another common-name collision; all 3 Pan Wu outliers — 3 different institutions; Wen-Jun Li's bacterial-typing review — CNRS Marseille vs. his Chinese institutions, lower-confidence split given topical adjacency). **Result:** 18 new ORCID-keyed confirmations + 14 new content-keyed splits, replayed through the full pipeline (`build_instances → apply_overrides → expand_targets → apply_overrides (2nd pass, since all 14 splits targeted expansion-sourced instances, which only exist after `expand_targets.py` runs) → link_instances → cluster_instances → mark_adjudication`); the §5g report went from 31 → **0** strong unreviewed outliers, adjudicated-cluster count unchanged at 201. **Pipeline gotcha discovered:** `build_instances.py` unconditionally `DETACH DELETE`s all `AuthorInstance` nodes including expansion-sourced ones, and `expand_targets.py` skips re-fetching any paper whose DOI already exists — so re-running `build_instances.py` on a graph that already has expansion data silently orphans those Paper nodes (author-less). Recovery: delete the orphaned `source:'expansion'` Paper nodes with no `WROTE` edges, then re-run `link_instances → cluster_instances` (to restore `cluster_id`, which `expand_targets.py`'s target-selection query depends on) before `expand_targets.py` can properly re-fetch them. **Do not re-run `build_instances.py` on a live graph that already has expansion data** — it's only safe on a fresh/seed-only graph.
   - **Corrections are data, not surgery:** `data/graph/identity_overrides.yaml` holds human `splits` (this authorship is a different person → neutralize its mis-assigned ORCID to `orcid_raw`) and `merges` (these are the same person → forced `human_verified` edge, conf 1.0). Matched by **content** (doi+name), **replayed every run**, so the graph is a pure function of *(OpenAlex source + overrides)* — idempotent, reversible, provenance-preserving. **Fixes = edit-file + recompute, never a live-graph edit.**

   **Pipeline (idempotent, run in order):** `build_instances.py → apply_overrides.py → link_instances.py → cluster_instances.py → mark_adjudication.py` (all in `graph_processing/`).

   **Deliberate trade-off:** genuine same-person links with no shared ORCID *and* no shared co-authors *and* different affiliations (e.g. two "Sixing Huang" papers from different career stages) are **not** drawn — the accepted false-negative cost of never asserting a merge. Downstream human/AI closes those via `merges`.

   **1c. Targeted graph expansion (added 2026-07-17, `graph_processing/expand_targets.py`).** Deliberately **not** a blanket "pull every seed author's full output" — that casts a big net senselessly. Instead, go after the **frequent-retraction authors and their high-impact not-yet-retracted papers**, since those are the actual review priority:
   - **Targets** = the top-N probable-person clusters (§2.1b) ranked by retracted-paper count (e.g. Raoult 28, Siba K. Samal 18, Ashok Pandey 12, …). A cluster qualifies as a target only if it resolves to a single ORCID (guaranteed by the ≥0.70 clustering threshold — two different ORCIDs never share a cluster).
   - **One OpenAlex call per target:** `/works?filter=author.orcid:<orcid>,is_retracted:false&sort=cited_by_count:desc&per-page=K` — the K most-cited non-retracted works for that person. K=25 by default; optional `--min-year` biases toward recent, reviewable work over old highly-cited reviews.
   - **One hop, then stop:** each new candidate paper's full authorship becomes `AuthorInstance` + `WROTE` + `AFFILIATED_WITH` (co-authors are needed for the co-author features), plus `PUBLISHED_IN`. Co-authors' *own* other papers are **not** recursively pulled — that boundary is what keeps this targeted instead of exponential.
   - **`CITES` edges** are added from each candidate to any paper **already in the graph** (matched on OpenAlex id) — this is the highest-value edge the expansion adds, since it directly powers the `retracted-citation-checker` sensor (Phase 4 #1) and the Tier-A "citations to retracted papers" feature.
   - **Validated (2026-07-17, top-4 targets, K=25):** 96 new candidate papers, 826 new authorship instances, 11 `CITES` edges to papers already in the graph — **7 to retracted papers, 4 to other not-yet-retracted candidates** (a citation chain of suspicion, not just single flags). Example: Siba K. Samal's *"Newcastle Disease Virus as a Vaccine Vector"* (144 citations) cites **four** different retracted NDV papers. New instances flow through the §2.1b identity pipeline unchanged (no new identity logic needed) — Raoult's cluster grew 227→252 instances, still one cluster; adjudicated count stayed exactly 201.
   - **Idempotent; MERGE throughout; tagged `source:'expansion'`** on new Paper/AuthorInstance nodes for traceability. Must run *after* `build_instances.py` (which rebuilds seed instances from `graph.json` and would otherwise wipe expansion authorships).
   - **Retraction status goes stale (found + fixed 2026-07-19, `graph_processing/refresh_retraction_status.py`).** `expand_targets.py`'s OpenAlex query filters `is_retracted:false` at fetch time, but OpenAlex's bulk search-filter index can lag its own individual-record data — 38 of 834 not-yet-retracted candidates were confirmed (live, zero lookup failures) to already be retracted per OpenAlex's per-DOI record, 17 of them "silently" (no "RETRACTED" title text at all, so a title-text check alone would undercount by ~45%). Surfaced incidentally while reviewing PubPeer comment data (several high-comment papers had "RETRACTED:" titles). Fix sets `is_retracted=true` + provenance fields but deliberately leaves `retraction_date` unset — neither OpenAlex nor Crossref reliably exposes one for these, and fabricating one would violate §0. Pool went 834→796; top-priority candidates were unaffected. **Run this after every `expand_targets.py` run** (added to the pipeline order below) — cheap (~5-10 min at OpenAlex's 10 req/s) and this staleness will keep recurring as OpenAlex's own data catches up over time.

   - **Editorial notices beyond retraction (added 2026-07-19, `graph_processing/refresh_editorial_notices.py`).** `is_retracted` only tracks full retractions; it has no concept of "formally flagged by the journal but not (yet, or ever) retracted." PubMed's E-utilities efetch exposes exactly that per-PMID via `CommentsCorrectionsList` (`ExpressionOfConcernIn`/`ErratumIn`/`RetractionIn`/`CommentIn` — only the first two are treated as integrity-relevant; `CommentIn` is ordinary editorial commentary). Measured on 738 not-yet-retracted candidates with a PMID: **227 (31%) have a live Expression of Concern**, 56 more have only an Erratum — both completely invisible before this. Also backfilled real, sourced `retraction_date`/`retraction_notice_doi` for 33 of the 38 papers `refresh_retraction_status.py` had corrected (deliberately left undated, since neither OpenAlex nor Crossref exposed one — PubMed did). **Caveat: 166 of the 227 EoC papers are one coordinated mass action by a single journal** (*New Microbes and New Infections*, tied to the IHU/Raoult ethics-committee protocol cluster) — each is still individually, formally EoC'd (a real per-paper fact), but the ranking will cluster on that journal as a result; read with that context, don't mistake it for 227 independent discoveries. Wired into `tier_a_scoring.py` as `pubmed_eoc_flag` (weight 2.5 — a formal dated journal fact, treated as strong evidence, unlike PubPeer comment counts which stay unweighted) and `pubmed_erratum_flag` (weight 0.3 — most errata are benign, kept low deliberately).

   - **ORI federal findings cross-check (added 2026-07-19, `graph_processing/refresh_ori_findings.py`).** `"Investigation by ORI"` was already one of our two `ADJUDICATION_REASONS` (§2.1b), sourced purely from Retraction Watch's own reason-code text — never cross-checked against ORI's actual published record. `ori.hhs.gov/content/case_summary` turned out to be the wrong source: it only lists respondents with a **currently active** administrative action, explicitly excluding anyone whose sanction period has expired — useless for historical cross-checking. ORI findings are also published permanently in the Federal Register under the standard title "Findings of Research Misconduct" (public, keyless API, no robots.txt restriction; confirmed 209 notices, 1999-2026), and critically **cite the affected papers by DOI directly in the notice body**. Extracted all DOIs from all 209 notices; only 2 matched our graph, both already-retracted: one clean validation (`10.1164/rccm.201903-0646oc`, already correctly tagged "Investigation by ORI" — now backed by a real citable federal source) and one genuine gap (`10.1073/pnas.0509351102` — a real ORI finding exists [Ariel Fernandez, 15-year debarment, image/data fabrication] but Retraction Watch's own reason tags for this DOI never included the ORI/misconduct-investigation codes, meaning **`on_misconduct_paper`/`mark_adjudication.py` is currently under-counting this case** — flagged, not yet fixed, since that touches the identity/adjudication layer rather than just Paper-node facts). Wired into `tier_a_scoring.py` as `ori_finding_flag` at weight 4.0 — the highest in the system, intentionally, since it's an adjudicated government finding naming the exact paper, not a proxy.
   - **Corrections/errata history (added 2026-07-21, `graph_processing/refresh_correction_history.py`).** Neither `is_retracted` nor the EoC/erratum fields above capture Crossref's own correction-notice chain. The obvious-looking implementation is a dead end: querying the *original* paper's own Crossref record (`/works/{doi}`) for a forward `relation.is-corrected-by` link returns `None` on every real correction chain tested (spot-checked against several PLOS ONE correction chains) — publishers deposit the *backward* link only, on the correction notice's own record (`update-to`), never the forward one on the original. The working mechanism is Crossref's reverse-lookup search filter, `GET /works?filter=updates:{doi}`, which returns every record whose own `update-to` names `{doi}` — confirmed live (e.g. `10.1038/s41586-025-09409-w`, an Author Correction, correctly resolves back to `10.1038/s41586-024-08248-5`, the paperconan-confirmed HDAC6 paper — see `runs/10.1038__s41586-024-08248-5/`). Only `{correction, corrigendum, erratum, addendum}` `update-to` types count here; `retraction`/`expression_of_concern`/`removal`/`withdrawal` are excluded since those overlap signals other fields already own. Measured on the full 795-candidate pool: **58 (7.3%) have at least one Crossref-linked correction**. Wired into `tier_a_scoring.py` as `crossref_correction_flag_count` at weight 0.3 (tied with `pubmed_erratum_flag` — a real, dated, publisher-deposited fact, but corrections are routinely benign, kept low deliberately).
   - **DOAJ live index replaces a static predatory-publisher guess (added 2026-07-19, `graph_processing/refresh_doaj_status.py` + `sensors/journal_integrity_check.py` redesign).** The old `PREDATORY_PUBLISHERS` check flagged every journal whose *title* contained a substring like "mdpi" or "frontiers" as high-severity "compromised peer review." Measured live: 16 of 17 journals it caught are actually properly DOAJ-vetted, including a false positive from pure name collision (`Frontiers of Environmental Science & Engineering` is Higher Education Press, not Frontiers Media). Replaced with a live DOAJ membership check (DOAJ's public Journal CSV — [doaj.org/csv](https://doaj.org/csv), ~23k journals, explicitly published for reuse) matched by ISSN (pulled per-journal via OpenAlex's Sources API, since our Journal nodes carry none) or exact title, scoped to known OA-only publishers matched on the graph's real `j.publisher` field (not journal title) so the same name-collision failure can't recur. Net result: 3 genuinely non-indexed OA-only-publisher journals flagged (down from 16+ false positives), severity downgraded high→medium (DOAJ absence alone doesn't warrant "high" the way an outright predatory-publisher match implied). **Compliance note:** doaj.org's robots.txt names and blocks ClaudeBot site-wide with no exception for the CSV path; user explicitly authorized fetching it anyway given it's a designated-for-reuse bulk file, not live-site crawling — re-download manually (`curl -sL https://doaj.org/csv -o data/doaj_journals.csv`) rather than having the script auto-fetch, so that judgment call is never made silently.

   **Pipeline (full, idempotent, run in order):** `build_instances.py → apply_overrides.py → expand_targets.py → refresh_retraction_status.py → refresh_editorial_notices.py → refresh_ori_findings.py → link_instances.py → cluster_instances.py → mark_adjudication.py`. `refresh_doaj_status.py` runs separately (journal-level, not part of the author/identity chain) whenever `data/doaj_journals.csv` is refreshed. `institution_retraction_rate.py` and `refresh_correction_history.py` (both added 2026-07-21), plus `publisher_retraction_rate.py`, `country_retraction_rate.py`, and `journal_retraction_rate_external.py` (all added 2026-07-22), also run separately, any time after `refresh_retraction_status.py` has settled the not-yet-retracted candidate pool and before `tier_a_scoring.py` — none of the five touch `AuthorInstance`/identity, so their position relative to `link_instances → cluster_instances → mark_adjudication` doesn't matter.

   **Full-graph snapshot for reproducibility (added 2026-07-21, `graph_processing/export_graph_snapshot.py` + `import_graph_snapshot.py`).** Rebuilding this graph from scratch on a new machine reruns this entire chain PLUS the Phase-4 sensor scan PLUS `gds_node_classification.py` — hundreds of live OpenAlex/Crossref/PubMed calls, each a lengthy, API-bound pass, just to reproduce a graph that already exists elsewhere. `export_graph_snapshot.py` dumps every node/relationship/property via APOC's *streamed* JSON export (`apoc.export.json.all(..., {stream:true})` — deliberately not the file-writing form, which is disabled by default and needs a DBMS restart to enable) to `data/graph/full_graph_snapshot.jsonl`, which travels with the rest of `data/` between machines the same way `.env.yaml`/`retraction_watch.csv` already do. `import_graph_snapshot.py` restores it onto a fresh Neo4j instance, matched on each label's natural key (`doi`/`name`/`institution_id`/`code`/`instance_id` — the same properties the `CREATE CONSTRAINT` statements above key on, never Neo4j's internal id), batched via `UNWIND`+`MERGE` like `expand_targets.py`'s own writer. **Verified live 2026-07-21:** round-tripped the full graph (21,674 nodes, 66,681 relationships, both scripts confirmed byte-for-byte-equivalent counts before/after) with zero duplication on a re-import (MERGE-idempotent). Re-run the export after any pipeline change that meaningfully updates the graph, so the snapshot doesn't go stale.

2. **Node property prediction — tiered, and be realistic.** Ship the explainable heuristic first; treat learned node classification as a later, separately-decided step.
   - **Tier A — heuristic graph features (implemented 2026-07-18, `graph_processing/tier_a_scoring.py` + `flag_evidence_report.py`).** Per not-yet-retracted candidate paper (834 in the current graph), the weighted score combines Phase-4 sensor flag counts with two **explainable graph features computed in pure Cypher** (no GDS needed for these): `coauthor_other_misconduct` (weight 1.5/co-author) — the candidate shares a `PROBABLY_SAME_AS` cluster with a co-author who wrote a paper retracted for a **misconduct-signal reason** (Paper Mill, Fabrication, Image/Results Manipulation, ...; the broad set, not the narrower `on_misconduct_paper` official-investigation/ORI-only flag — see §2.1b, and note this is content-adjacent to but distinct from that field), and `journal_retr_rate` (weight 2.0) — the paper's journal's measured retraction rate in this graph. Every point still maps to a named, sourced 🚩 (`flag_evidence_report.py` names the specific co-author and the specific misconduct DOI/reason). **Measured impact:** on the top-50 ranking, `coauthor_other_misconduct` reshaped the order significantly — e.g. a Leishmania donovani paper (`10.3389/fimmu.2018.00063`) jumped to rank 2 on the strength of 6 named co-authors sharing a cluster with a Manipulation-of-Images retraction, a signal the sensor-only score had been blind to (only 1 citation-based flag). (e) citations-to-retracted-papers is covered by the existing `retracted-citation-checker` sensor reading `CITES` edges (§2.1c). (b) shortest-path-distance remains unbuilt (reserved); (d) shared-institution was BUILT 2026-07-21, superseding this reservation (see update below); (f) publication-burst also unbuilt.
   - **Update (2026-07-21): shared-institution built + a fourth Tier-A weighted feature.** `graph_processing/institution_retraction_rate.py` fills reservation (d) above: per-`Institution` retraction rate (retracted papers involving it / all papers involving it, via `(Paper)-[:INVOLVES]->(Institution)`), taking the **max** across a paper's institutions rather than the average — one high-risk institution is a real signal even if a paper's other affiliations are clean, and averaging would dilute exactly the case worth flagging. Wired into `tier_a_scoring.py` as `institution_retr_rate` (weight 1.5). **Caveat, not a bug:** because `INVOLVES` only exists where Retraction Watch itself recorded an Institution, almost every paper touching a given institution in this graph is by construction a retracted seed paper — so institutions with several linked papers skew heavily toward a 100% rate (the same base-rate distortion `journal_retr_rate` already has). The institution's `paper_count` is stored alongside the rate — not a hidden threshold — precisely so a reviewer can judge whether a rate rests on 1 paper or 20. Measured on the 795-candidate pool: 251 (32%) have a nonzero `institution_retr_rate`, mean 0.129. The same date's `refresh_correction_history.py` (§2.1c above) adds the Tier-A score's fourth weighted feature, `crossref_correction_flag_count`.
   - **Update (2026-07-22): external, unbiased publisher/country rates — fixing the bias the caveat above names.** Prompted by reading arxiv 2602.19197 (a bibliometric fraud-detection paper using publisher-level retraction rates and country-level co-authorship anomalies from external sources). `graph_processing/publisher_retraction_rate.py` and `graph_processing/country_retraction_rate.py` replace `journal_retr_rate`/`institution_retr_rate`'s graph-scoped denominator with a real-world one: **numerator** = the FULL `retraction_watch.csv` (71,106 records, all subjects — deliberately NOT filtered to `data.seed_subset` the way `extract_enrich.py`'s ingest is), grouped by its own `Publisher`/`Country` columns; **denominator** = Crossref's Members API (`counts.total-dois`, publisher-level) or OpenAlex's `works?filter=institutions.country_code:XX` (country-level works count). Both are free, keyless (mailto-only polite pool). Wired into `tier_a_scoring.py` as `publisher_retr_rate` (weight 1.5) and `country_retr_rate` (weight 1.0, lowest of the three `_retr_rate` sensors — broadest, least specific attribution). **The name-matching problem (publisher only):** `Journal.publisher` holds Crossref-canonical legal names, but Crossref's own Members API `query` search is unreliable for them — some real publishers return zero results outright ("Springer Nature", "Taylor and Francis"), and it frequently ranks an unrelated same-named small member above the true one (querying "Wolters Kluwer" ranked two <500-DOI subsidiaries above the real 2.97M-DOI "Ovid Technologies (Wolters Kluwer Health)"). Fixed with a retry-on-empty-results strategy plus a **strict token-subset test** (every significant word of the query name must appear in the candidate's name, not just any one shared word) before accepting a fuzzy match — a looser "shares one word" rule produced a real false positive (`Cell Press` → an unrelated `Japan Society for Cell Biology`, both sharing only the token "cell", implying an impossible 44% rate) before this was tightened. No publisher name aliasing was needed for `country_retr_rate` — RW's country column is resolved to ISO2 via `pycountry.countries.search_fuzzy()` (with a small hand-checked alias table for the ~16/184 strings it can't resolve, e.g. "Turkey", "Myanmar (formerly Burma)"), matching `Institution.country`'s existing ISO2 format directly. **Absence of a confident match is stored as `null`, never a guessed `0.0`** (plan.md §0) — 58/101 graph publishers and 109/112 graph countries resolved to a rate on the 2026-07-22 run; the rest (e.g. Cell Press, BioMed Central, Taylor & Francis — all genuinely hard to resolve to a single Crossref member) are left unmatched and logged. **Measured, and notably NOT distorted like the graph-internal rates:** real-world rates are two to three orders of magnitude smaller than `institution_retr_rate`/`journal_retr_rate` (top publisher Hindawi at 8.4%, matching its well-documented 2023–2024 mass-retraction event; typical country rate 0.01–0.3%, topped by Saudi Arabia at 0.33%) — the score contribution is correspondingly small in absolute terms, but the *ordering* it induces is a genuine, non-graph-scoped signal. A statistical-outlier test for reason-code concentration (chi-square goodness-of-fit vs. a field-scoped RW baseline, or a Bayesian alternative — the user does not want a frequentist/Fisher's-exact approach) was discussed the same session but deliberately NOT built; noted here for later consideration.
   - **Update (2026-07-22): journal-level external rate, on request ("can we calculate journal_retr_rate the same way as publisher_retr_rate?").** `graph_processing/journal_retraction_rate_external.py` applies the identical publisher_retraction_rate.py methodology at journal granularity, stored as `journal_retr_rate_external` (weight 2.0, same tier as the existing graph-internal `journal_retr_rate`, which stays as-is — both are kept, one graph-relative, one real-world-scale, same relationship as `institution_retr_rate`/none-yet at institution level). **Numerator:** the FULL `retraction_watch.csv`, grouped by its own (single-valued, not semicolon-list) Journal column. **Denominator:** Crossref's Journals API (`GET /journals?query=...`, returning `counts.total-dois` per journal, confirmed live — e.g. Cell, ISSN 0092-8674: 26,637 total-dois). **Name matching is markedly better-behaved here than at publisher level:** journal titles are far more standardized across sources than publisher legal names, so a direct normalized-exact-match against the csv's own Journal column resolves 541/636 (85%) of this graph's journals with zero fuzzy logic — publisher-level needed the alias/substring machinery precisely because legal entity names vary so much more. The remaining ~15% fall back to the same bidirectional-substring matching as publishers, and the Crossref Journals API search has the same relevance-ranking blind spot documented for Members API (querying bare "Cell" doesn't surface the real Cell journal in the top 20 results at all, even though it exists under its own ISSN — confirmed live) — mitigated with the identical strict token-subset + max-total-dois + floor + impossible-rate-discard algorithm as `publisher_retraction_rate.py`, reused essentially verbatim.
   - **Update (2026-07-22, same session): graph-internal `journal_retr_rate` REMOVED from Tier-A scoring/display — user feedback.** The line above said `journal_retr_rate` "stays as-is alongside" the new external one; the user immediately pushed back that showing both made them "uneasy" since they overlap. Correct call: `journal_integrity_flag_count`'s check 3 (`sensors/journal_integrity_check.py`) is a >10%-retraction-rate threshold on the SAME underlying fact `journal_retr_rate` measures continuously (both computed from our own seeded graph) — a journal could trip both signals from one real cause, double-counting it in the score. Removed `journal_retr_rate` entirely from `tier_a_scoring.py`/`flag_evidence_report.py`/`build_review_page.py` (WEIGHTS, QUERY, calculate_score, output, the review-card row); `journal_retr_rate_external` is the real, non-redundant replacement. The property itself is UNTOUCHED in `gds_node_classification.py` — it's still a Tier-B GDS input feature there, just no longer surfaced/scored in Tier-A. Also added a caveat directly on the "Journal integrity flag" row/JSON note (in both `build_review_page.py` and `flag_evidence_report.py`) spelling out that the flag can fire for any of 3 different reasons (DOAJ-non-indexing, Scopus/WoS delisting, or the retraction-rate threshold) before showing the paper's specific reason — general lesson: a `flag_count` field bundling multiple distinct checks under one name/weight is worth calling out explicitly to the reviewer, not just documented in code comments.
   - **Update (2026-07-22, same session): capped scoring for publisher/country/journal_retr_rate_external — fixes a real dominance bug the weight-retuning couldn't.** The retuning history in `config/weights.yaml` (x1 → x100/x1000 → x10/x10/x1000) chased the same underlying problem back and forth without solving it: these three rates are heavy-tailed (most candidates ~0.01%-1%, a handful of Hindawi-family journals 8%-26%), so no single LINEAR weight can both differentiate the normal range and avoid letting the extreme tail dominate — turn it up enough to matter typically and the tail explodes (at x100, `journal_retr_rate_external` alone hit +52 for the top journal, some papers reached #1 on venue alone); turn it down to tame that and it goes invisible for everyone else (at x10, "barely play a role"). Root-caused and fixed properly: added `capped_contribution(key, rate)` in `tier_a_scoring.py` — `contribution = min(rate * weight, cap)` — with a new `CAPPED_KEYS` map and `_cap` weight-keys (`publisher_retr_rate_cap`/`country_retr_rate_cap`/`journal_retr_rate_external_cap`, currently 2.0/1.5/2.0) in `config/weights.yaml`. Weights went back up (publisher/journal to x100, i.e. 100; country stayed at 1000) to properly differentiate the normal range, while the cap ensures no venue-level fact can ever outrank direct per-paper evidence (ORI finding, own AI-text tells) regardless of how elevated the rate — the same "same person/place ≠ same responsibility" principle §0 already applies to `coauthor_other_misconduct`, extended here to venue-level ecological facts. `build_review_page.py`/`flag_evidence_report.py` both import `capped_contribution` (not just `WEIGHTS`) so the displayed/reported per-row score always matches what's actually summed, and show a "capped at N" note when the ceiling was hit (confirmed live: 191 papers hit the 2.0 publisher/journal cap, 79 hit the 1.5 country cap, out of 795 candidates). The raw, uncapped rate is still shown in the evidence text either way — only the SCORED contribution is capped.
   - **Update (2026-07-22): institution-level global retraction count — a raw count, not a rate.** The user asked for the institution-level equivalent of the publisher/country work above ("how often does each institute have retractions"). `institution_retraction_rate.py` now also computes `global_retraction_count`: an exact-string match of `Institution.name` against the FULL `retraction_watch.csv`'s own Institution column (all subjects, not seed-scoped). **Coverage is honestly partial, and worth recording why:** it was initially assumed (wrongly) that `Institution.name` is created directly from the csv's raw Institution text, which would make an exact match lossless — checked empirically and this is false. `Institution` nodes are actually built from each author's **OpenAlex canonical institution** (`build_instances.py`/`expand_targets.py`; `extract_enrich.py`'s own `csv_institutions` field captures the csv's raw per-paper text but is never used to create `Institution` nodes). The csv's own Institution column is a free-text "Department, Institution, City, CountryCode" string per author (e.g. `"Stroke Medicine, University Hospital Coventry and Warwickshire NHS Trust, Coventry, GBR"`), so an exact match only hits when a large, simply-named university's OpenAlex canonical name happens to appear verbatim inside that longer string (confirmed hits: Harvard University, Cornell University, University of Michigan, University of South Florida, ...) — hospitals/departments/institutes whose csv text differs from their OpenAlex form (a same-institution example found live: OpenAlex's "University Hospital Coventry" vs. the csv's "University Hospital Coventry and Warwickshire NHS Trust") correctly do NOT match, rather than risk a wrong one. Measured: only 130/2,992 graph institutions get a nonzero count, even though 1,993 of them are provably tied to an actual RW-seeded retraction (i.e. genuinely do appear in the csv, just under a different string) — this is the exact same name-matching problem `publisher_retraction_rate.py` solves with substring/token matching, just at a scale (133,025 distinct csv institution strings vs. ~1,700 for publishers) not worth hand-tuning right now. **Deliberately stored as unscored context only** (no reliable external "total papers by this institution" denominator exists to turn it into a rate, and a prolific university like Harvard will have a large count for reasons unrelated to risk) — surfaced in `build_review_page.py`/`flag_evidence_report.py` next to `institution_retr_rate`, never added to `tier_a_scoring.py`'s `WEIGHTS`.
   - **Update (2026-07-22): first/last-author personal retraction rate — `graph_processing/author_retraction_rate_external.py`.** User's proposal, after discussing whether to score authors the same way as journals/publishers/countries: restrict to FIRST and LAST authors only, since they're conventionally the ones directly responsible for the work (unlike `coauthor_other_misconduct`'s broader "any co-author sharing a cluster" scope). **Why this couldn't just reuse the venue-sensor recipe:** those match a NAME (journal title, publisher legal name, country) against `retraction_watch.csv`'s own free-text columns — workable because there are only hundreds/thousands of fairly unambiguous venues. A person's name has no such safety net — RW's `Author` column is bare semicolon-separated free text with **no identifier at all**, so name-matching risks silently pooling unrelated people who share a common name, a much higher-stakes mistake than mismatching a journal since it wrongly implicates one specific named individual. Fixed by resolving BOTH sides by **DOI, never by name**, and restricting to authors who carry an ORCID (`AuthorInstance.orcid`/`.has_orcid`): **denominator** = the ORCID Public API's own `/works` endpoint for that exact ORCID (`orcid_client.py`'s `get_claimed_dois()`, already built for the unrelated OpenAlex-mis-assignment cross-check — reused here for a new purpose); **numerator** = how many of those claimed DOIs appear in the FULL `retraction_watch.csv`'s `OriginalPaperDOI` column. Since the numerator is a strict subset of the denominator by construction, the rate can never exceed 100% and needs none of the venue sensors' ">1.0 implausible rate" discard logic. A floor (`MIN_CLAIMED_WORKS=3`) skips authors with too few claimed works for a meaningful rate; below `LOW_CONFIDENCE_CLAIMED_WORKS=10` the rate is shown but flagged low-confidence. Authors without an orcid on file are left `null`, never guessed (§0). **Coverage measured 2026-07-22:** 1,359/2,067 (66%) first-author instances and 1,620/1,994 (81%) last-author instances carry an orcid (1,994 distinct ORCIDs total); after the floor, 1,291 produced a rate, giving **626/795 (79%) of not-yet-retracted candidates** an `author_retr_rate_external` signal (avg 6.5%), with 7 ORCID-API lookup errors (409s, ~0.4%) out of 1,994 calls. Top case: 9/11 ORCID-claimed works retracted (81.8%), a real, independently checkable fact. **Weighted at 2.0, deliberately NOT capped** unlike `publisher_retr_rate`/`country_retr_rate`/`journal_retr_rate_external`: those three needed a hard ceiling because their rates sit on a ~0.01%-1%-typical/8%-26%-extreme-tail scale, forcing a 100x-1000x weight multiplier that would otherwise let the tail explode. An author's personal rate is already on the same [0,1] percentage scale as `institution_retr_rate` (single digits to tens of percent typical) — no multiplier, no cap needed, so it sits in that same "graph feature" weight tier instead. Still flagged with the §0 "same person ≠ same responsibility" caveat in the UI/JSON notes — this is the author's rate across ALL their claimed work, not a finding about this specific paper. **Retuned same day, twice.** First pass: weight lowered 2.0 → 1.0 on "one last or first author can score 1 solid point" — read (wrongly) as capping a RATE-based contribution at 1 point for a theoretical 100% rate. **User then clarified the actual intent** with a concrete example: "for 10.1016/j.fuel.2021.121859, Jingshan Shi has 4 retracted works when he was 1st or last author, that should add 4 points" — i.e. score by the raw retracted-works COUNT, not the rate. Rate-based scoring badly undercounted a prolific repeat offender already present in the data: an author with 50 of 133 claimed works retracted (37.6% rate) scored only +0.376, when the underlying fact — 50 confirmed retractions — clearly warranted far more. Fixed by changing `calculate_score()` (`tier_a_scoring.py`/`flag_evidence_report.py`/`build_review_page.py`) to use `author_retr_rate_external_n` (the count) instead of `author_retr_rate_external` (the rate) as the multiplied quantity — weight stayed at 1.0 (now "1 point per retracted work" rather than "ceiling at 100% rate"), so Jingshan Shi's 4 retracted works now score exactly +4.00. The rate is still displayed in the evidence text for context. Deliberately left uncapped at first: a serial-offender author was allowed to dominate the score on this signal alone, per the explicit, stated intent. **That uncapped version was then shown to the user with its actual real-world effect before shipping it further:** Didier Raoult has 248 of his ORCID-claimed works matched to Retraction Watch, so as last author he contributed +248 to every paper he's last-author on -- the entire top 5 of the ranking became his papers (250+ each) vs. the next tier at ~15-20, i.e. the same "one signal swamps everything else" dominance bug the user had already flagged and asked to be capped for publisher/journal/country rates, just from a different source. Asked the user directly whether this was the intended outcome; **answer: no, cap it to 10.** Added `author_retr_rate_external_cap: 10.0` and folded `author_retr_rate_external` into the existing `CAPPED_KEYS`/`capped_contribution()` machinery (same mechanism as publisher/country/journal_retr_rate_external, just applied to a raw count instead of a [0,1] rate) across `tier_a_scoring.py`/`flag_evidence_report.py`/`build_review_page.py`. General lesson repeated from the earlier capping episode: an uncapped, unbounded-tail count/rate should be surfaced to the user WITH its concrete real-data consequence before being treated as final, not just implemented literally and left as-is. **Retuned a third time, same day:** user proposed RATE × 100 instead of COUNT × 1 (capped at 10) — e.g. "Mohammad J. Taherzadeh has 8 of 560 ORCID-claimed works retracted (1.4%)" should add 1.4 points, not 8. Switched `calculate_score()` back to `author_retr_rate_external` (the rate) with weight raised to 100 — the existing `author_retr_rate_external_cap: 10.0` needed no change, since `capped_contribution()` already ceilings whatever's passed through it regardless of whether that's a count or a rate. Net effect: this signal now shares the exact same weight (100) and cap (10) shape as `publisher_retr_rate`/`journal_retr_rate_external` instead of being a one-off "N retracted works = N points" idiom. Also on this pass: the evidence text changed from "{name} has {n} of {total} ORCID-claimed works retracted per Retraction Watch ({rate})" to "{n} out of {name}'s {total} ORCID-claimed works were retracted ({rate})", moving "per Retraction Watch" out of the visible row text and into the hover-help popover (alongside the existing "matched by DOI, not by name" explanation) — same pattern as the "capped at N" note's move into the info popover earlier that session.
   - **Update (2026-07-22): three new review-page filter chips, no scoring change.** "Main authors of retracted works" (`author-retr` tag, whenever `author_retr_rate_external_n > 0` -- 394 papers), "High retract rate journal"/"High retract rate publisher" (`journal-high-retr`/`publisher-high-retr`, `journal_retr_rate_external`/`publisher_retr_rate >= HIGH_EXTERNAL_RATE_THRESHOLD = 0.05` -- a deliberately conservative cutoff well above the ~0.01%-1%-typical range, catching only genuinely elevated/well-documented cases like Hindawi's 8.4%; 176/3 papers respectively). Also **"PubPeer allegation"** (`pubpeer-allegation`, 120 of the 126 PubPeer-commented papers): stricter than the plain "PubPeer" chip (any comment, including a methodology question or the author's own rebuttal) -- fires only when at least one comment's `categorize_pubpeer_comments.py` category is a substantive claim about the paper (`official_editorial_action`/`external_investigation_reference`/`conflict_of_interest`/`reference_integrity`/`image_integrity`), excluding `methodology_question` (a clarification request) and `author_response`/`uncategorized` (no claim, or no rule matched). Same "nature, not verdict" discipline as the underlying categorizer -- still just a filter, never scored.
   - **Update (2026-07-22): `graph_processing/pull_retraction_watch_csv.py` — manual, on-demand refresh of `retraction_watch.csv` from skill_worth_exploring.md's "RW Database daily CSV" recommendation.** Deliberately NOT daily/cron'd and NOT added to `review/pipeline_app.py`'s stage registry, per the user: they want to **freeze the current snapshot to publish an article against a known, reproducible dataset**, and only pull a fresh copy when they explicitly choose to. Pulls from Crossref's GitLab mirror (`gitlab.com/crossref/retraction-watch-data`), same schema already read via `.env.yaml`'s `data.retraction_watch_csv`. Safety, since this one csv is now the numerator behind five already-scored/shown signals (institution/publisher/country/journal/author retraction rate, all added this session): downloads to a temp file first; refuses to swap in a download missing any of the columns those sensors actually read (`CORE_COLUMNS`), or whose row count drops >1% vs. the current file (`DROP_TOLERANCE` — RW only grows day to day, so a shrink means a bad/partial fetch, not real data); archives the file being replaced, timestamped, before ever overwriting it (`retraction_watch/archive/`), so the exact snapshot behind a past run/publication stays recoverable. Does NOT re-run any downstream sensor itself — pulling fresh data and re-scoring against it are two separate, deliberate steps; the script prints the exact re-run order (institution → publisher → country → journal → author rate scripts, then scoring/report/review-page) as a reminder, but leaves acting on it to the user. Verified live via `--dry-run`: current snapshot 71,106 rows (2026-07-16), upstream currently at 71,231 (+125) — confirmed the checks and reporting work without touching the frozen file.
   - **Update (2026-07-22): `graph_processing/hijacked_journal_checker.py` — Retraction Watch / Anna Abalkina "Hijacked Journal Checker" (skill_worth_exploring.md's "Hijacked Journal Checker," user-requested build).** Cross-checks this graph's 636 `Journal` nodes (by normalized name or `journal_crossref_issn`) against the public Google Sheet of 456 documented journal-hijacking cases (scam sites cloning a real journal's name/ISSN to solicit fraudulent "publications" — a case DOAJ-seal/Scopus-WoS-delisting checks can't catch, since the hijack doesn't touch the real journal's own standing). Pulled live via the sheet's CSV export endpoint, no auth. **Deliberately NOT scored, diverging from the doc's own suggestion to feed this into `journal_integrity_flag_count` as a 4th check:** many sheet rows use the IDENTICAL title/ISSN for both the clone and the real journal (that's literally what a hijack is), and this graph dedupes `Journal` nodes by name/ISSN with no per-paper publisher-domain data to tell which "instance" a given paper actually came from — scoring it risked penalizing a paper legitimately published in the real, unhijacked journal for an unrelated bad actor impersonating its name elsewhere (wrong-direction false positive). Kept as unscored review context instead (same bucket as `institution_global_retraction_count`/GDS prior), showing both the real site and the clone site so a reviewer can check which one a given paper actually links to. Measured 2026-07-22: 5/636 graph journals matched (Fuel, Investigative Ophthalmology & Visual Science, Jundishapur Journal of Microbiology, Korean Journal of Physiology and Pharmacology, Proceedings of the National Academy of Sciences — all plausible typosquat/impersonation domains, e.g. `sciencedirects.com` vs. the real `sciencedirect.com`), covering 27/795 not-yet-retracted candidates.
   - **Update (2026-07-22): `graph_processing/known_miller_coauthor_checker.py` — "Known-miller co-author list" (skill_worth_exploring.md §C, user-requested build).** Flags candidate papers co-authored by a person NAMED in investigative reporting as running/participating in a paper mill — complements `coauthor_other_misconduct` (which only fires for co-authors of *retracted* papers) by covering millers whose own output hasn't been retracted, or isn't even in this graph. Seed list `data/known_millers.csv` (name/orcid/source_url/note), sourced from Retraction Watch's 2026-07-21 investigation "Exclusive: Medical student in Nepal behind busy research factory" (Raghabendra Kumar Mahato, network founder, confirmed ORCID `0009-0003-5685-7118`; Shreya Singh Beniwal, co-administrator, no confirmed ORCID). **Matching is ORCID-only, same discipline as `author_retraction_rate_external.py`:** confirmed concretely while sourcing this exact list — a live web search for "Shreya Singh Beniwal" surfaced a DIFFERENT, unrelated "Shreya Singh" (a computer-science academic) with her own distinct ORCID, exactly the name-collision risk that makes name-only person-matching unsafe. Beniwal stays in the csv for documentation but is skipped for graph matching entirely (never guessed by name, plan.md §0). Unscored context, same reasoning as `journal_hijack_flag` above (guilt by co-authorship with a bad actor is associative, not a finding about this paper). **Measured 2026-07-22: 0 hits** — neither miller's ORCID nor name appears anywhere in this graph, expected since this corpus is microbiology-scoped and the source investigation's output is clinical-cardiology/case-report-focused. Built as reusable, low-maintenance infrastructure for when the corpus expands past microbiology or new documented millers are added to the csv — not because it currently finds anything.
   - **Update (2026-07-22): `ai_text_tell_detector.py` moved out of the routine rerun sequence (user judgment call, not a bug fix).** After the Cabanac cross-check (below) also came back 0/109, the user questioned whether phrase-based AI-tell detection is worth the ~30min/run it costs at all, given modern models rarely leave "as an AI language model"-style boilerplate and both careless users and paper mills have had since 2023 to learn to strip it. Confirmed empirically: `ai_text_tell_detector` had already scanned the FULL corpus (`review/pipeline_app.py` passes `--sample 800` > 795 candidates, so this isn't a coverage artifact) and found **0 hits, 0 papers**. Agreed this is a real, shrinking-yield signal, not a bug — kept the code (still runnable manually, still worth a shot on a future corpus with different discovery-era characteristics) but set `optional=True` on its `pipeline_app.py` Stage entry, same mechanism already used to exclude `reference_integrity_checker` from "run all"/"run category" batches. Its `data/flags/ai_text_tell_flags.json` output, `ai_text_tell_flag_count` scoring, and review-card display all stay wired — nothing about the sensor itself changed, only whether it fires automatically during a routine rerun.
   - **Update (2026-07-22): `graph_processing/cabanac_chatgpt_checker.py` — Cabanac ChatGPT-writing list (skill_worth_exploring.md's "Cabanac ChatGPT-writing list," user-requested build).** Guillaume Cabanac's Problematic Paper Screener (PPS) "Suspect" detector, server-side filtered to its ChatGPT fingerprint category. **No plain export exists:** the PPS page is an Oracle APEX interactive report rendered client-side via AJAX (confirmed via `curl` — the initial HTML just says "Crunching data..."), so the list was captured via browser automation: load `https://dbrech.irit.fr/pls/apex/f?p=9999:25::::RIR:IRC_TORTUREDPHRASES:ChatGPT` (the site's own "Filter the table below" deep link for this category), expand to all rows (109, confirmed via the report's own "1-109 of 109" summary), and read the rendered DOI/fingerprint-text/PubPeer-link cells. Frozen as `data/cabanac_chatgpt.csv` (doi, title, year, fingerprint_evidence, pubpeer_url, pubpeer_users) — same "pull once, freeze, no silent auto-refresh" discipline as `retraction_watch.csv` and the Hijacked Journal Checker list. 107/109 rows carry a live PubPeer link (2 don't yet, so the doc's "each entry has a PubPeer comment" isn't literally 100% true — close enough to keep the source description, but worth knowing). **Matching is pure DOI-to-DOI, no name-matching at all** — safer even than the known-miller/author-rate sensors, since there's no person-identity question here whatsoever. **Deliberately unscored**, for a different reason than the ecological-evidence caveat that applies to journal-hijack/known-miller: this IS direct, paper-specific evidence (not ecological), but it's the *same underlying phenomenon* `ai_text_tell_flag_count` already scores from full text — an external confirmation of AI-generated text, not a new category of evidence. Scoring both would double-weight the same phenomenon differently depending on which of two sources happened to catch it first. Used instead to calibrate/cross-check `ai_text_tell_flag_count`'s own hit rate, matching the doc's stated purpose. **Measured 2026-07-22: 0/109 matched in this graph** — expected; the PPS ChatGPT-category list skews heavily CS/engineering/social-science (titles like "Bio-inspired artificial synapses," "IoT-Based Smart Road," "E-learning environment in Egypt"), essentially no microbiology-corpus overlap, same domain-mismatch story as the known-miller checker. Built as reusable infrastructure for corpus expansion.
   - **Update (2026-07-22): replaced fixed-cap scoring with corpus-relative MINMAX scoring for all five entity-level retraction-rate signals (`institution_retr_rate`, `publisher_retr_rate`, `country_retr_rate`, `journal_retr_rate_external`, `author_retr_rate_external`) — user proposal.** User's framing: "instead of capping, minmax those scores... take the worst offender's retraction rate as max and score it as 20, everybody else gets a score proportional to that max" (e.g. an 80%-rate author scores 20, a 20%-rate author scores 5). Replaces `capped_contribution()`'s `min(rate * weight, cap)` (the 2026-07-22 caps update earlier this session) with `minmax_contribution()`: `(rate / worst_in_corpus) * target` (target = 20 for all five, configurable per-signal in `config/weights.yaml` as `<key>_minmax_target`). Requires a new precomputation step, `compute_corpus_maxes(rows)`, run once per script against the FULL not-yet-retracted candidate population (never a `--top`-sliced subset, or the anchor would shift with an unrelated CLI flag) — added to `tier_a_scoring.py`, `flag_evidence_report.py`, and `build_review_page.py`'s `main()`, each right after fetching `rows` and before any scoring. `institution_retr_rate` (previously a bare linear weight, uncapped) was folded into the same mechanism on request — clarified via AskUserQuestion, since the user's opening sentence said "those currently with caps" (only 4) but the example list also named "institute"; user chose to include all 5. **Checked before shipping** (same "surface the concrete consequence" discipline as the capping episode): all five current worst-in-corpus values are backed by reasonably large samples, not a fragile single-paper artifact (author 43.5% at n=10/23 claimed works, institution 95.7% at n=46 papers [King Saud University], publisher 8.4% at n=11,524 [Hindawi], journal 31.0% at n=157, country 0.33% at n=2,418 [Saudi Arabia]) — so the mechanism is safe to ship as-is today. **Flagged, not yet resolved:** minmax is inherently MORE sensitive to a future single extreme outlier than a fixed cap was, since one new noisy data point becomes the anchor for every other entity's score in that category, not just its own — worth re-checking whenever these sensors are re-run against new data. **Also flagged in `config/weights.yaml`:** a target of 20 for all five is well ABOVE every direct-evidence signal in the system (`ori_finding_flag`/`paperconan_confirmed` at 4.0, `pubmed_eoc_flag` at 2.5) — in tension with the system's own stated principle that ecological/entity-level evidence should never be able to outrank direct per-paper evidence. Confirmed live: paper #1 in the current ranking (score 58.87) gets 55.37 of that (94%) from the five minmax signals combined, only 3.50 from direct per-paper evidence (an Expression of Concern + a journal-integrity flag) — this is an intended consequence of the user's literal "score it as 20" instruction, not a bug, but surfaced here in case 20 was meant as "comparable to the strongest signals" rather than "stronger than all of them." Lower the `_minmax_target` values (e.g. to ~2-4, matching the old caps' scale) if that's not the intended tradeoff. Verified live in the browser: the review page's per-row help popovers now read "Minmax-scaled: the worst-in-corpus value for this signal (X%) scores 20; this row scores proportionally less," and `flag_evidence_report.py`'s JSON output carries `corpus_max`/`target_max_score` per signal for full transparency.
   - **Update (2026-07-22, same day): retuned pubmed_eoc_flag and all five minmax targets, on user feedback that the flagged 20-vs-4.0 tension above was too extreme.** `pubmed_eoc_flag` raised 2.5 → 10.0 (Expression of Concern is a formal, dated, journal-issued fact -- user judged it deserved more weight than a weak signal like `pubmed_erratum_flag`'s 0.3). All five `_minmax_target` values lowered 20.0 → 10.0. Net effect: the worst-in-corpus offender in any of the five entity-level categories can still outscore a single `ori_finding_flag`/`paperconan_confirmed` (4.0), but now ties `pubmed_eoc_flag` (10.0) rather than dwarfing it 8x over -- narrows the tension flagged above without fully resolving it (ecological evidence can still, in principle, outrank direct per-paper evidence at the extreme). Verified live: `tier_a_scoring.py`'s current #1 candidate dropped from 58.87 to 38.68, matching the arithmetic exactly (each minmax contribution halved, EoC contribution quadrupled). Same session: user also floated adding a "Select all" button to the review page's filter-chip row, to select every chip then deselect unwanted categories -- flagged that the chips currently combine with AND ("a paper must carry every selected tag"), so selecting most/all of them would show ~0 papers; would need OR semantics when multiple chips are active for the described workflow to make sense. **User chose to abort this idea for now** rather than resolve the semantics question -- not built.
   - **Update (2026-07-22, same day): `institution_retraction_rate_external` — fixes the "correct but too high to be intuitive" graph-internal institution rate, user request.** The user's exact framing: institution_retr_rate's graph-internal rate (top offenders at 85-96%) is technically correct (real ratio of retracted/total papers involving that institution, in THIS graph) but unintuitive at that scale -- the same inflation problem journal_retr_rate had before journal_retr_rate_external replaced it. Checked feasibility before building: every Institution node already carries a ROR ID (`institution_id`, 2992/2992 = 100% coverage) from OpenAlex at ingest time, and OpenAlex supports direct ROR-based institution lookup (`GET /institutions/ror:<id>` -> `works_count`) -- confirmed live (St Vincent's Hospital Sydney -> 14,616 works). This means, unlike publisher/country/journal, the denominator side needs NO name-matching/search/alias-table work at all -- only ~130/2992 institutions (those with a nonzero `global_retraction_count`, i.e. an exact-string hit in the full Retraction Watch csv) need an OpenAlex call, since a zero numerator makes the rate zero regardless of denominator. Extended `institution_retraction_rate.py` (not a new file, since it already computes the numerator) to add `institution_retr_rate_external` = `global_retraction_count / OpenAlex works_count`, propagated to `Paper` via the same reset-then-max-across-INVOLVES idiom as the other four. **institution_retr_rate (graph-internal) and institution_global_retraction_count both DEMOTED to unscored review-card context** (same fate as the old graph-internal journal_retr_rate) -- `institution_retr_rate_external` is the real, minmax-scored replacement in `tier_a_scoring.py`/`flag_evidence_report.py`/`build_review_page.py` (MINMAX_KEYS's `institution_retr_rate` entry repointed to `institution_retr_rate_external`). **Measured 2026-07-22:** top external rate is European Society of Cardiology at 0.101% (n=1/994) -- same ~0.001%-0.1% real-world scale as publisher/country/journal, confirming the fix; coverage is 28/796 not-yet-retracted candidates (same partial-coverage ceiling as `global_retraction_count`, since both share the same exact-string-match numerator bottleneck -- "missing != zero", plan.md §0).
   - **Update (2026-07-22, same day): live "exclude ecological signals from score" toggle on the review page, user request (prompted by the 20-vs-4.0 tension discussion above).** User wanted a quick way to compare a paper's rank/score with vs. without the five entity-level minmax signals (author/institution/publisher/country/journal retraction rate), to judge how much of a given score rests on association rather than direct evidence -- this is what motivated the earlier-aborted "chip select-all" idea, once its actual goal was clarified. Implemented as a single checkbox in `build_review_page.py`'s toolbar (not per-signal toggles -- user chose the single "all 5 at once" option over 5 separate switches). Server-side: `score()` refactored to expose `minmax_total(r)` (the sum of all 5 `minmax_contribution()` calls) as its own function; each card's HTML now carries both `data-score` (full) and `data-score-nominmax` (`score() - minmax_total()`) attributes, computed once at build time. Client-side JS (`toggleMinmax()`): re-sorts the `cards` array by whichever score is active, physically re-appends the DOM nodes in the new order (`listContainer.append(...cards)` -- necessary because `renderPage()`'s existing show/hide-by-pagination logic relies on DOM source order for on-screen position, it never reorders nodes itself), then recomputes each card's displayed rank/🚩-gauge/score number in place (`updateCardDisplays()`, mirroring `flag_gauge()`'s exact FLAG_BANDS thresholds in JS). Verified live in the browser: toggling correctly swaps the #1 card to a different paper with its `data-score-nominmax` value displayed and the 🚩 gauge recalculated, and un-toggling reverts to the original order exactly.
   - **Tier B — GDS node classification (built + evaluated 2026-07-18, `graph_processing/gds_node_classification.py`).** Neo4j GDS 2026.05.0 was confirmed installed (`gds.version()`); a full `gds.beta.pipeline.nodeClassification` pipeline was built: project a paper-paper graph (edges = shared-probable-person co-authorship [~53k pairs] + `CITES` [82], undirected) → FastRP(dim 64) + degree + PageRank node embeddings, combined with the same domain features as Tier A → auto-selected LogisticRegression/RandomForest → train/predict. **Labeling design (avoids the obvious leakage trap):** trained only on the 1,244 *retracted* papers, class 1 = misconduct-signal reason (281, 22.6%) vs class 0 = other retraction reason (963) — since both classes are already "near a retraction" by construction, the expansion-sampling bias does not separate the two training classes, so this labeling is not leaky for this specific target (unlike training retracted-vs-not directly, which would be). **Measured result — a clear negative/weak finding, kept because it settles the question for future readers:** held-out test accuracy 0.823 (barely above the 0.774 majority-class baseline), F1(misconduct-class) only 0.353 (precision 0.833, recall 0.224 — misses ~78% of misconduct-labeled test cases). Applied to the 834 candidates, `gds_misconduct_prob` never exceeds **0.54** (mean 0.38, stdev 0.11) — the model stays unconfident on every candidate, the domain-shift signature you'd expect training-on-retracted/predicting-on-not-yet-retracted. Top-ranked candidates by this prior visibly cluster by author-neighborhood (Leishmania/kala-azar group, NDV-vaccine group) rather than by any interpretable fraud signal — i.e. FastRP is substantially learning "which co-authorship neighborhood is this," which the explainable `coauthor_other_misconduct` feature already captures directly and legibly. **Decision: keep `gds_misconduct_prob` wired into the graph and the triage CSV as a labeled, non-scored secondary column only — it never enters the weighted Tier-A score.** Re-evaluate if the labeled-retraction set grows substantially (currently 1,244 papers is thin for FastRP+RF) or if a differently-shaped target (e.g. predicting retraction reason *category* rather than a binary) is tried.
3. **Label carefully.** Positive class = papers retracted for *misconduct* reasons (Paper Mill, Fabrication, Image Duplication, Compromised Peer Review), **not** all retractions. This is the concrete payoff of the §0 framing. (Confirmed as the Tier-B label design above.)

*Exit criteria:* a scored table of candidate papers with per-feature contributions. **Met 2026-07-18** — `data/tier_a_triage_top50.csv` (score + full signal breakdown, including the `gds_misconduct_prob`/`gds_flagged` secondary columns) and `data/flag_evidence_report_top.json` (named, sourced evidence per contributing signal) cover all 834 not-yet-retracted candidates.

### Phase 3 — Numeric forensics with paperconan (Claude skill)
**Deliverable:** a numeric-forensics report for each top-N suspicious paper *that has machine-readable data files*.

- **What paperconan actually does (verified against its `detectors.md`):** it is a *numeric-forensics* skill, not a general "read the paper" AI. It runs ~30 detectors over the paper's **raw data tables** — the supplementary `.xlsx`/`.csv` grids — looking for fabrication fingerprints: identical/constant-offset/constant-ratio columns, repeated values, decimal-tail clustering, cross-sheet value reuse, and **GRIM/GRIMMER** checks (whether a reported mean/SD is even arithmetically possible for the stated n). Its own principle is "signal not verdict", which matches §0.
- **It is complementary to Phase 4, not overlapping.** paperconan inspects the *numbers inside the data files*; the Phase 4 sensors inspect the paper's *citations, in-text statistics, and online reputation*. Run both. Note the one adjacency: paperconan's GRIM/GRIMMER + decimal-clustering are statistical checks in the same family as Phase 4's `p-value-hacking-detector`, but they test a different thing (arithmetic consistency of summary stats vs. p-values clustering under 0.05) — no redundancy; keep both.
- **Input requirement changes the handoff.** paperconan needs the **supplementary data files**, not the PDF prose or just a DOI. So the Phase 2 → Phase 3 queue must point at (or fetch) each paper's supplementary datasets. **Coverage is inherently limited:** many papers ship no machine-readable data (figures only, PDF tables, or nothing), and those simply can't be run through paperconan — that's expected, not a bug. Record "no data files available" as a distinct outcome in the UI rather than a clean pass.
- **Supplementary-data availability flag (added 2026-07-19, `sensors/suppl_data_check.py`).** This is the gate that answers "which papers can paperconan even run on?" up front, as one shared cached fact per `Paper` node, instead of each file-dependent sensor re-discovering it. Source: **Europe PMC's REST web service** (EMBL-EBI, keyless, the `/europepmc/webservices/` path IS the designated API — no robots concern like the DOAJ bulk file). Its `core` result carries a per-article `hasSuppl` (Y/N) flag; for `hasSuppl=Y` papers, the companion endpoint `…/rest/{pmcid}/supplementaryFiles` returns the actual files as a ZIP (verified `content-type: application/zip`), so a Y gives the forensic sensor a **direct download URL, not just a boolean**. Written as a **four-state** fact, not a boolean — and crucially, **`hasSuppl=Y` does NOT mean "downloadable"**: the OA `supplementaryFiles` ZIP endpoint only serves articles in Europe PMC's open-access subset, and 404s for others even when `hasSuppl=Y`+`inPMC=Y` (proven live). So the sensor **HEAD-checks the ZIP endpoint (must return 200) before marking a paper downloadable**. States: `pmc_suppl` (ZIP verified 200 — the only state that sets `has_pmc_suppl`+`pmc_suppl_url`), `suppl_not_downloadable` (`hasSuppl=Y` but ZIP 404s — exists per record, not fetchable here), `no_pmc_suppl` (in PMC, none found), `unknown` (not in PMC — a soft negative, not a real "no"). Fields on `Paper`: `pmc_suppl_status`, `has_pmc_suppl`, `pmc_suppl_url`, `suppl_checked_date`. **Verified live on all 796 (2026-07-19): 86 `pmc_suppl` (downloadable — the real Phase-3 coverage number), 2 `suppl_not_downloadable`, 318 `no_pmc_suppl`, 390 `unknown`.** (A pre-verification pass over-counted at 88; 2 had dead ZIPs.) paperconan's actual runnable set is those 86 (`MATCH (p:Paper {has_pmc_suppl:true}) RETURN p.doi, p.pmc_suppl_url`). The wrapper `runs/run_paperconan.py` is PMC-first — it downloads that ZIP itself, unzips into the run's `data/`, then scans (see Phase 3 above).
- **How it runs:** it's a Claude skill, not a library the pipeline imports. It runs inside Claude — a **Claude routine** (scheduled) or an **interactive session** — on the top-N. The pipeline only *produces the queue and consumes the verdict*: (a) Phase 2 writes the top-N (with pointers to their data files) to a small file/table the routine reads; (b) paperconan runs per paper and returns its report; (c) that report is written back onto the `Paper` node / results table for the UI. Keep the exchange as plain files/DB rows so it works identically whether a routine or a human triggers it. Run only on the top-N (it's expensive). No API key in `.env.yaml` — paperconan uses whatever model the Claude session/routine runs under.

### Phase 4 — Flag sensors as Claude skills
**Deliverable:** reusable skills, each emitting structured `{flag, severity, evidence, source_url}`.

Each skill is an independent "sensor" that adds 🚩s. **Design rule:** each returns *evidence, not a verdict*; the scoring engine turns skill output into 🚩 counts. Sensors are grouped by signal-per-effort. Which reasons they target is anchored to the measured Retraction Watch reason counts for the bio slice — Paper Mill (11,796), compromised/concerns peer review (~11k), Computer-Generated Content (9,144), image duplication (5,458), plagiarism euphemisms (3,747) — so we prioritize sensors that hit the biggest categories.

**Core set (original four)**
1. **`retracted-citation-checker`** — does the paper cite any paper that is `is_retracted` in our graph (or in Retraction Watch)? Strong signal if citing *after* the retraction date.
2. **`p-value-hacking-detector`** — scan reported p-values for suspicious clustering just under 0.05 (p-curve / caliper test on extracted stats). Flags "0.048, 0.049, 0.047" patterns. *Distinct from paperconan's GRIM/GRIMMER + digit stats (different test — keep both).*
3. **`pubpeer-comment-scanner`** — query PubPeer for the DOI; surface community comments (PubPeer has an API/lookup). Comments there are the single highest-yield human signal.
4. **`web-suspicion-search`** — web search for the title/DOI + "concerns/retraction/image duplication" on blogs, forums, For Better Science, etc. Lower precision; tag as weak.

**Tier 1 — high signal, cheap, build right after the core set**
5. **`tortured-phrases-detector`** — paper-mill fingerprint #1. Detects garbled paraphrase synonyms ("bosom peril" = breast cancer, "counterfeit consciousness" = AI) via the known tortured-phrase list (Cabanac's Problematic Paper Screener). Near-zero false positives. Targets the 11,796 paper-mill reason.
6. **`ai-text-tell-detector`** — regex/LLM scan for AI-generation giveaways left in published text ("As an AI language model", "Regenerate response", "Certainly, here is", "my last knowledge update"). Very high precision. Targets the 9,144 Computer-Generated Content reason.
7. **`reference-integrity-checker`** — resolve every cited reference against Crossref/PubMed. *Nonexistent* references signal AI-fabricated/mill papers; broken/mismatched ones are a softer flag. Complements #1 (that checks citation *validity*; this checks *existence*).

**Tier 2 — field-specific, high value for microbiology**
8. **`nucleotide-sequence-sanity`** (Seek-&-Blastn style) — extract stated nucleotide sequences (primers, siRNA/shRNA) and BLAST them; flag where the sequence doesn't match the gene it's claimed to target (the Byrne & Labbé method). Uniquely powerful for molecular-biology/microbiology gene papers, a large share of mill output. Higher effort: needs sequence extraction + BLAST API.
9. **`cell-line-and-reagent-check`** — cross-check named cell lines against the ICLAC register of misidentified/contaminated lines; validate that antibody/reagent RRIDs and catalog numbers exist. Bio-specific, moderate effort.
13. **`code-integrity-check`** (PROPOSED 2026-07-19, from the user's own experience) — for **microbial genomics / bioinformatics** papers, audit the linked analysis **code** for bugs that could change a conclusion. This is a large, underserved error class distinct from every other sensor (they inspect the paper's *outputs*; this inspects the *process that produced them*): the Excel gene-name auto-corruption problem (Ziemann et al. 2016 — `SEPT2`→a date, widespread), 0- vs 1-based genome-coordinate mixing, wrong reference build/strand, uncorrected multiple testing, silent parse failures, non-deterministic ordering (`sort` without `-n`), pinned-vs-drifting dependencies. Tiered by signal-per-effort:
   - **Tier 1 — code-availability integrity (cheap, high precision; the code analog of `reference-integrity-checker`).** Extract code-host URLs (github/gitlab/zenodo/codeocean) from full text + Data/Code-Availability statements; verify via the host API that the repo **exists, is public, non-empty, and has real commit history** (not a single dump), ideally with a license. Flags "code available at [dead / empty / private / 404] repo" — a concrete reproducibility red flag. Low FP.
   - **Tier 2 — known-footgun static scan (moderate).** Clone the repo and scan for domain-specific conclusion-changing patterns above (spreadsheet reads of gene columns, coordinate-system mixing, hardcoded reference paths, missing multiple-testing correction, results committed with no code to regenerate them), plus generic linters (ruff/pyflakes, R `lintr`) as weak smells. Signal, not verdict — most linter output is noise.
   - **Tier 3 — agentic conclusion-affecting review (aspirational; a `paperconan`-style handoff).** An agent reads the analysis code **against the paper's specific claims** and flags logic that could change a reported result — the part that genuinely needs understanding *both* code and paper, exactly where an agent is uniquely suited. Runs as a Claude-skill workflow like paperconan (draft → human adjudication → **not-scored**), reusing `runs/run_paperconan.py`-style archiving.
   - **Discipline & fit:** a bug ≠ a wrong conclusion ≠ misconduct (§0 — usually an honest error); stays **not-scored**, adjudicated. **Coverage caveat (measured 2026-07-19):** only ~5 of 835 cached papers link code and 8% carry a code/data-availability statement — because this *retraction-seeded* corpus skews older + wet-lab. The payoff scales with the **computational microbial-genomics slice** this seed underrepresents. Reuses `data/full_text_cache/` for URL extraction.
   - **Corpus analysis (RW, 2026-07-19/20) — the deeper finding:** RW has no genomics/bioinformatics token, but **`(BLS) Genetics` = 9,277 (4.4× Microbiology's 2,100)** is the genomics proxy and is far more *error-driven* (**63% error-type retractions** vs 42% Microbiology; **72%** for the ~289 Genetics×computational slice, dominated by "Unreliable Results/Conclusions" + "Concerns about Data" — the code-bug signature). **BUT a code-link probe killed the obvious "seed from Genetics" plan:** a sample of 30 *recent* (≥2016) computational-Genetics retractions → 19 had OA full text → **19/19 carried an availability statement but 0/19 linked a code repo** (verified: they're *data*-availability — GEO deposits, "supporting information", "on request" — NOT code). Genomics culture **deposits data (mandated: GEO/SRA/GenBank), it does not share analysis code.** So a Genetics expansion would *not* make this sensor fire either.
   - **The corpus where it actually fires: bioinformatics TOOL/METHODS papers, seeded by JOURNAL not subject.** A paper introducing a pipeline/tool ships its GitHub repo as the deliverable (~100% code-linked), and some journals mandate code. Target venues: *Bioinformatics, BMC Bioinformatics, Genome Biology, GigaScience, Nature Methods, PLOS Computational Biology*. RW's subject taxonomy can't isolate tool papers, so seed the code-integrity corpus by **journal**, not by `(BLS)` subject. (Still aligns with the genomics/computational expansion — [[project_scope_microbiology_first]] — just via venue, not subject.)
12. **`image-reuse-screen`** (BUILT 2026-07-19, `sensors/image_forensics/`) — the biggest hole paperconan explicitly leaves (it doesn't touch images), and the exact gap the HDAC6 image-duplication case exposed. Vendored (MIT) from [1anj/academic-integrity-skill](https://github.com/1anj/academic-integrity-skill) — the geng/耿同学 whistleblower's toolkit — but **only** its generic `image_similarity_screen.py` (average-hash + flip/rotate variants + Hamming). Evaluated live: 0 false positives across 30 real figures, correctly catches exact + mirror-flipped duplicates; self-limited to whole-image reuse (misses cropped/partial-panel). Runs inside `runs/run_paperconan.py` on the figures already fetched from PMC, so an images-only ZIP (that paperconan can't scan) still gets a forensic result; summary lands in the run's `meta.yaml image_screen:` block and on the review card as a `🖼` badge + not-scored context. The wet-lab screens (`blot_gel_lane_audit`, `microscopy_reuse_screen`, `flow_plot_duplicate_screen`) assume the input IS that assay type and false-positive to BLACK on ordinary figures (verified: 44 spurious findings on non-blot images). **Figure-type router BUILT 2026-07-19** (`sensors/image_forensics/figure_classifier.py`): a conservative Pillow-only gate (white/dark/colour fractions → `chart_or_text` / `color_image` / `grayscale_image`) that routes each specialized screen to ONLY its matching type; ambiguous composites/charts route to nothing. Validated: the 44-FP composite now routes to nothing (0 findings), a synthetic gel → blot audit, dark fluorescence → microscopy. The wet-lab screens are now vendored but **opt-in (`--wetlab`) and only ever run through the router**; results archived to `data/audit/wetlab_screens.json`. Flow plots aren't reliably pixel-detectable, so never auto-routed. All image findings stay **not-scored** (signal, not verdict — aHash flags legitimately-similar images too).

**Tier 3 — contextual / metadata**
10. **`journal-integrity-check`** — is the journal predatory, absent from DOAJ, or **delisted from Scopus/Web of Science**? Delisting often follows mill infiltration. Also flag implausibly fast submission→acceptance turnaround (proxy for the ~11k compromised-peer-review cases).
11. **`impossible-values-check`** — logical impossibilities only: percentages >100, negative counts, SD=0, means outside the possible range, sample sizes that don't reconcile across tables. *Keep to logical impossibilities — leave GRIM/GRIMMER, terminal-digit, and decimal-clustering statistics to paperconan (Phase 3) to avoid duplication.*

**Deliberately out of scope (reserved slots, not built in POC)**
- **`image-duplication-flag`** — high value (5,458 image-duplication retractions) but needs figure extraction + forensics; reserve the slot.
- **Hyperprolific-author / co-authorship-cluster signals** — belong in Phase 2 graph features, not a per-paper skill.
- **Any statistic paperconan already computes** (GRIM/GRIMMER, terminal-digit, decimal clustering) — redundant.

**Recommended minimal high-yield set to ship first:** core #1–#4, then Tier 1 (#5–#7), then #8 as the field-specific centerpiece once the basics work.

### Phase 5 — HTMX review frontend
**Deliverable:** a triage table for a human reviewer.

- **Backend:** FastAPI serving Jinja2 templates + HTMX partials, reading from the graph + a small results table.
- **Table columns:** Title · DOI (link) · Journal · Risk score (🚩 count, sortable, default sort = most flags first) · per-flag breakdown (hover/expand for evidence + source links) · paperconan AI opinion (expandable) · **reviewer decision** (buttons: `Legit` / `Needs deeper look` / `Confirmed problematic`).
- HTMX handles: sort, expand-row for evidence, and posting the reviewer decision back (which becomes labeled training data for Phase 2 Tier B — closing the loop).
- **Copy discipline:** header reads "Papers flagged for human review". No cell ever asserts fraud. Each 🚩 links to its evidence.

**Increment 1 built 2026-07-19 (`graph_processing/build_review_page.py`) — read-first static page, no backend yet.** Renders the top-N Tier-A queue as a single self-contained `review/index.html` (inline CSS/JS, no external resources). **Private local file, never a published/shared artifact** — it shows misconduct-adjacent flags tied to real named researchers, so per §7 (keep the review queue private during POC) and §0 it is written to disk for the user to open locally. Lives in a top-level `review/` dir (moved out of `data/` 2026-07-19); the generated HTML is git-ignored via `review/*.html` while a tracked `review/README.md` documents the privacy intent. Score is shown as a 1–5 🚩 priority gauge (documented bands: ≥12→5, ≥9→4, ≥6→3, ≥3→2, >0→1) with the exact weighted score kept under each gauge and driving the sort — honouring plan.md's original "🚩 count" idiom. Mirrors `tier_a_scoring.py`'s WEIGHTS exactly; each card expands into named, sourced evidence for every contributing signal — including the two 2026-07-19 additions the older `flag_evidence_report.py` predates (Expression of Concern with dated notice DOI; PubPeer comment count + category breakdown + author-response note, shown as non-scored review context). The co-author-proximity block carries the §0 "same person ≠ same responsibility" caveat inline. Verified end-to-end via a local HTTP server + Playwright (render, filter, expand, and the reviewer-decision buttons). **Reviewer decisions persist to browser `localStorage` only** — clearly labelled as a preview; durable/shared decisions (and the Tier-B training-data loop) need the FastAPI+HTMX backend, which is the next Phase-5 increment. paperconan opinion column also still pending (depends on Phase 3 coverage, currently ~nil).

**Increment 2 (first draft BUILT 2026-07-21, `review/pipeline_app.py`): pipeline-execution frontend.** Every stage used to be run by hand, in the right order, from the CLI — the identity/expansion chain (§2.1c: `build_instances.py → apply_overrides.py → expand_targets.py → refresh_retraction_status.py → refresh_editorial_notices.py → refresh_ori_findings.py → apply_overrides.py (2nd pass) → link_instances.py → cluster_instances.py → mark_adjudication.py`), then Phase 4's `sensors/*.py → wire_sensor_flags.py`, then `gds_node_classification.py`, then `tier_a_scoring.py`/`build_review_page.py`. No orchestrator existed (BUG.md #11), and this was a real, not just theoretical, footgun: **confirmed live 2026-07-21** — a full Neo4j rebuild silently skipped `expand_targets.py` and the three `refresh_*.py` stages (only surfaced when a user asked about one specific DOI's correction history and its absence didn't add up), and separately left the entire Phase-4 sensor layer + `gds_node_classification.py` unrun (surfaced only when the reviewer noticed familiar tags like `COAUTHOR_OF_RETRACTED_WORKS` missing from the review page — both gaps sat silent through a full pipeline "completion" until incidental discovery).

A FastAPI+HTMX dashboard (`review/pipeline_app.py`, separate from `build_review_page.py`'s static triage page above) now makes every stage a button: a fixed, hardcoded registry (no arbitrary commands — no injection surface) covering the full identity/expansion chain, all 7 Phase-4 sensors, scoring/GDS, and the reproducibility snapshot scripts, each run as a background subprocess with live HTMX polling (`hx-trigger="every 2s"` while running) and a captured output log. "Run this section" / "Run FULL pipeline" run a whole category or the entire non-optional pipeline in order, stopping on first failure; `reference_integrity_checker.py` and the snapshot scripts stay manual, single-stage opt-ins (same routine-rollout exclusions as elsewhere in this doc). Last-run status (idle/running/done/failed, timestamp, duration) persists to `data/pipeline_run_history.json` across app restarts. Verified live: ran `retracted_citation_checker.py` end-to-end through the dashboard (idle → running → done in 0.2s, captured output matched a direct CLI run exactly).

**First-draft limits, deliberately left for later:** plain background threads, not a real task queue (fine for one local operator, not concurrent multi-user use); ordering is *shown* (numbered, grouped by category) but not *hard-enforced* — a genuinely destructive stage (`build_instances.py` run on a graph that already has expansion data) gets an `hx-confirm` browser prompt naming the exact risk, but nothing blocks running stages out of order beyond that. The reviewer-decision backend (durable, shared decisions closing the Tier-B training loop) remains a separate, still-unbuilt piece of Phase 5 — this dashboard is purely operational (run stages), not a review tool.

---

## 5. Suggested milestones / order of work

1. **M1:** CSV → graph for Microbiology-only (2,100 rows). Inspect with a few Cypher queries. *(Phase 1)*
2. **M2:** OpenAlex enrichment for those authors; graph grows to include non-retracted papers. *(Phase 2.1)*
3. **M3:** Tier-A heuristic scoring + a static HTML dump of the top 50. *(Phase 2.2 + minimal 5)*
4. **M4:** Wire in the cheapest high-signal sensors first — `retracted-citation-checker` (+ its `external-retracted-citation-checker` sibling, added 2026-07-20), `pubpeer-comment-scanner`, `tortured-phrases-detector`, `ai-text-tell-detector` (all low-effort, all hit top reasons). Then `nucleotide-sequence-sanity` as the microbiology centerpiece. `reference-integrity-checker` is built and can be run manually, but is **held out of the routine rollout** (2026-07-20): NOT scored (its no-DOI bibliographic-search route has a ~71%-of-corpus false-positive rate — see `tier_a_scoring.py` WEIGHTS comment) and slow (~2hrs for the full corpus even with 3x concurrency, since Route 2's Crossref+PubMed fallback fires per no-DOI reference). Revisit once Route 2 is fixed or dropped. *(Phase 4)*
5. **M5:** FastAPI + HTMX interactive table with reviewer decisions. *(Phase 5)*
6. **M6:** paperconan skill (routine or interactive session) deep-dive on top-N; embeddings/classifier if warranted. *(Phase 3 + 2.3)*

Ship M1–M3 before adding sophistication; each milestone is independently demoable.

---

## 6. Open questions to resolve early

- **Supplementary-data acquisition for paperconan** — *resolved what it needs* (machine-readable `.xlsx`/`.csv` data files, not the PDF), *and now resolved the fetch-at-scale + coverage question* (2026-07-19, `sensors/suppl_data_check.py`, see Phase 3 note): Europe PMC's `hasSuppl` flag + a **ZIP-endpoint-verified** `…/supplementaryFiles` download gives a per-paper availability fact and a direct URL (`hasSuppl=Y` alone is NOT enough — the OA ZIP endpoint 404s for non-OA-subset articles, so the sensor HEAD-checks it). **Verified coverage: 86 of 796 active candidates have downloadable PMC suppl files.** The paperconan handoff is now built (`runs/run_paperconan.py`, PMC-first: downloads the ZIP, unzips, scans). Remaining open part: the 390 `unknown` (non-PMC) papers — a publisher-specific fetcher could recover some, but PMC covers the fetchable-today set.
- **Enrichment sources — decided: OpenAlex primary, PubMed secondary.** OpenAlex gives ORCID + per-author affiliation + citation edges in one keyless call; PubMed is the fallback for missing records and a cross-check. Still confirm OpenAlex journal coverage for your target sources.
- **PubPeer access** — **RESOLVED (2026-07-18).** No browser needed: Phase 1 uses PubPeer's own browser-extension lookup endpoint (`POST /v3/publications?devkey=PubMedChrome`, batch DOIs → per-DOI `total_comments` + canonical `/publications/<id>` URL); Phase 2 fetches `/publications/<id>` directly (server-rendered, embeds full comments as JSON). The site's own `/api/search/` is Turnstile-gated and unusable in automation. robots.txt (`Allow: /`, `Disallow: /search`, `Crawl-delay: 10`) is honored by a 10 s site-wide limiter + permanent resolution cache. Implemented in `sensors/pubpeer_comment_checker.py`; config in `.env.yaml` `pubpeer:`.
- **Identity resolution** — **RESOLVED (2026-07-17, see §2.1b): never merge.** Instance-based model (one `AuthorInstance` per authorship) + a reversible `PROBABLY_SAME_AS` confidence layer + soft clustering into "probable persons"; human corrections via `identity_overrides.yaml`, replayed every run. ORCID is treated as a low-trust signal because OpenAlex mis-assigns it, so conflicting ORCIDs *lower confidence* rather than veto a link.
- **Graph engine** — *updated 2026-07-17:* running on **Neo4j** for now (see §2 engine update) — all of §2.1b/c is built and live there; `.env.yaml` `graph_db.engine` defaults to `"neo4j"`. Original reasoning (start on LadybugDB, revisit at Tier B/C) is preserved in §2 for reference; Tier B/C is moot on the engine question now since Neo4j + GDS was the eventual Tier B/C target anyway. Schema remains portable either way.

---

## 7. Risks

- **False accusations / defamation.** Mitigated by §0 framing, human-in-the-loop gate, evidence-linked flags, and never publishing scores as conclusions. Keep the review queue private during POC.
- **Name-collision errors** inflating an author's apparent retraction history. Mitigated by DOI/PMID/ORCID-based identity, weak-weighting name-only matches, and the no-merge instance model (§2.1b).
- **OpenAlex ORCID mis-assignment.** OpenAlex sometimes stamps a same-named person's ORCID onto the wrong paper, which can attach a retraction (or misconduct finding) to an innocent researcher. Mitigated by the instance-based no-merge model (§2.1b): mis-assignment is a *visible, droppable* edge with provenance (`orcid_raw`), never a silent merge; the `coherence_outlier` flag surfaces the same-ORCID case for human review; conflicting-ORCID pairs get lowest confidence. **Detection is imperfect** — a career move looks identical to a mis-assignment (e.g. Ashok Pandey NIIST→IITR), so it is always human-reviewed, never auto-corrected. The authoritative check is the researcher's own ORCID works list, but that only *confirms* (absence ≠ mis-assignment, since ORCID records are often incomplete).
- **Overfitting to Retraction Watch's discovery bias** — the DB reflects what *got caught*, skewed toward certain publishers/regions. A model trained on it predicts "resembles a caught paper", not "is fraudulent". State this limitation in any writeup.
- **Skill precision** — web-search and p-value sensors are noisy; weight them low and always show evidence.
- **Update (2026-07-22): `country_retr_rate_minmax_target` lowered 10.0 -> 2.5 after live regression — country-level minmax was dominating the whole ranking.** User noticed live: with the include-signals toggle on, the top of `review/index.html` was almost entirely Saudi-Arabia-affiliated papers. Root cause: unlike institution/publisher/journal/author (where the worst-in-corpus value is held by 1-4 papers), country is coarse enough that its worst-in-corpus holder (Saudi Arabia, 0.33% national retraction rate) is shared by 62 candidate papers — under minmax's winner-take-all scaling, ALL 62 got the full target score regardless of their own paper-specific evidence. **Bayesian shrinkage toward the global mean was checked and explicitly ruled out**, not just skipped: SA's rate rests on a large, low-variance sample (2,418 RW records / 734k OpenAlex works; CN's is 37,620/16.7M) — shrinking with any prior strength small enough to be statistically justified doesn't change SA's rank at all (checked C up to 100,000, an order of magnitude past any single country's real sample size); SA only drops behind CN once C reaches ~1,000,000+, at which point the "prior" is simply overriding the data, not denoising it. So the dominance was never a sampling-noise artifact — it's a structural interaction between minmax's all-or-nothing scaling and country's coarse granularity. Fixed by cutting country's target specifically (others left at 10.0, since their max-holders are much more paper-specific). **Also caught in the process:** `data/tier_a_triage_full.csv` had gone stale — `tier_a_scoring.py`'s default invocation (no `--output`) only prints top-50 to stdout; the actual routine command is `--top 500 -o data/tier_a_triage_full.csv` (see `pipeline_app.py`'s `tier_a_scoring` Stage). Re-ran with the correct flags and regenerated `review/index.html`; confirmed live that the new top-30 is a mix of SA/LB/FR/CN/SN/IN instead of ~all-SA.

- **Update (2026-07-22): review page chips split into "scoring signals" vs "filters", and the two chips that used to double as filters (author-retr/journal-high-retr/publisher-high-retr) became live score-toggles — user request.** Previously every chip in `build_review_page.py` was a show/hide filter (AND-combined). User asked to make chips control scoring instead. Landed as: chips tied to an actual scored weight (SCORED_TAGS) render highlighted by default and, when clicked off, zero that signal's contribution across every paper and re-rank live via a new `data-contribs` JSON blob baked into each card (nothing gets hidden); chips with no scored weight (PubPeer count, journal-hijack, known-miller, Cabanac ChatGPT-tell, self-cite, suppl, image, country dropdown) keep the old filter behavior, since there is nothing to zero out. The two chip types are now rendered in visually separate labeled groups ("Scoring signals" / "Filters") per a follow-up request, so the two mental models never mix in one row. **Then fully closed out the institution/country asymmetry**: those two entity-rate signals never had their own chip (unlike author/journal/publisher), and were instead covered by a bundled 5-signal "include institution/country retraction-rate signals" switch left over from the earlier minmax work. Added two new calibrated chips instead of leaving the switch in place: `institution-high-retr` (presence-only, rate > 0 — checked live that only 4% of the 796 not-yet-retracted candidates have ANY external institution-level retraction record at all, so presence alone is already rare/notable, same reasoning as the existing author-retr chip's n>0 condition) and `country-high-retr` (`HIGH_COUNTRY_RATE_THRESHOLD = 0.00125`, calibrated separately from the journal/publisher chips' 5% cutoff since country_retr_rate lives two orders of magnitude smaller — checked that country_retr_rate>0 for fully 34% of candidates, too broad to badge as "notable" on its own, so used the measured top-10%-by-rank cutoff (0.126%) rounded to a clean 0.125%, catching ~80/796 candidates instead of a third of the corpus). The old bundled switch, its checkbox/CSS, and its JS (`includeEntitySignals`/`toggleEntitySignals`) were removed entirely now that all 5 minmax entity-rate signals have individual, independently togglable chips.

- **Update (2026-07-22): fixed a chip-coverage gap (`crossref_correction_flag_count` had no chip at all) and added a "Clear all" action — both user-noticed.** User deselected every scoring chip and found "Crossref correction notice (3)" still contributing — turned out `correction_count` was scored in `score()` since early in the project but had never been wired into TAG_LABELS/SCORED_TAGS/the folded-card badges loop, so there was no chip to zero it with (also had no collapsed-card badge at all, only ever visible in the expanded evidence panel). Added a `correction` chip (58/795 candidates) alongside the other 14 scoring chips, closing the gap — every scored signal in `score()` now has a corresponding chip. Also added a "Clear all" link next to the Scoring signals group label (`clearAllScoreChips()`): distinct from the existing "reset chips" button (which restores full default — all scoring chips back on, all filters cleared), this only zeroes every scoring chip at once and leaves filters untouched, for the opposite question ("what's left with every scored signal stripped out?").
